/**
 * `@bb/shared-types` — wire-level types shared across services and clients.
 *
 * Public surface kept intentionally narrow ; add a new module per concern
 * (sprint Pro League adds `match-event.ts`, future sprints will add
 * betting markets, gazette articles, etc.).
 */

export * from './match-event';

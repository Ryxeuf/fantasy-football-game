# Sprint 27 — Evolutions & confort

> Duree cible : ~6 jours (allegé : replay sharable retiré, hot-reload
> Docker remonte en S24.9)
> Theme : preparer la suite long-terme et boucler la dette mineure.
> Pre-requis : S24 (DX hot-reload), S25 (logs/perf), S26 (refactor +
> retention).

## Taches

| # | Tache | Cat | Effort | Statut | Detail |
|---|-------|-----|--------|--------|--------|
| S27.1 | Compétitions esport mensuelles "Nuffle Cup" + bracket visuel + match-of-week | EVO | L | [x] | **TERMINE en S27.1a-j.** S27.1a (foundation) — helpers `nuffle-cup-monthly.ts` + champs `monthlyYear`/`monthlyMonth` sur Cup + migration. S27.1b (endpoint listing) — service `cup-monthly-listing.ts` + route `GET /cup/monthly`. S27.1c (UI calendrier) — page publique `/cups/monthly`. S27.1d (badge champion backend) — service `cup-championships.ts` + `cupChampionships` dans `GET /coach/:slug`. S27.1e (badge UI) — `CoachCupChampionshipsBanner.tsx` sur `/coach/[slug]`. S27.1f (match-of-the-week lecture) — champs `featuredAt`/`featuredNote` sur LocalMatch + service `match-of-the-week.ts` (`getCurrentMatchOfTheWeek()`) + route `GET /cup/match-of-the-week`. S27.1g (admin pick) — `setMatchOfTheWeek({matchId, note})` defensif + route `POST /cup/match-of-the-week/:matchId` (admin only). S27.1h (UI banner match-of-the-week) — `MatchOfTheWeekBanner.tsx` sur `/cups/monthly`. S27.1i (creation cup mensuelle admin) — `createCupSchema` etendu + handler `POST /cup` propage si admin sinon 403. S27.1j (bracket visuel) — composant `apps/web/app/cups/[id]/CupBracketView.tsx` (vue chronologique des matches, mise en evidence vainqueur via `data-winner='A'|'B'|'draw'`, masque score si pending), branche additivement dans `/cups/[id]/page.tsx`. 7 nouveaux tests UI. |
| S27.2 | Mobile parite : Action Picker Throw Team-Mate + flow 2-clics | AMELIO | M | [x] | `apps/mobile/app/play/[id].tsx`. Refrappe ce qui a ete fait en S23 sur web (commit `28c31e4`). Backend deja pret (B2.4). **TERMINE : S27.2.1 helper pur `processThrowTeamMateClick` (4 etats discriminants, 6 tests). S27.2.2 helper `canActivateThrowTeamMate` + integration page : state `currentAction` + `throwTeamMateThrownId`, branche `processThrowTeamMateClick` au debut de `onCellClick` (priorite sur MOVE/BLOCK), bouton "Lancer un coequipier" dans actionsBar (apparait si selected player a au moins 1 legal `THROW_TEAM_MATE` et flow inactif), bouton "Annuler (lancer/cible)" pendant le flow (label change selon Phase 1/2). 4 tests `canActivateThrowTeamMate`. Total 10 tests. Les autres popups (block/push/follow-up/reroll) etaient deja parite par `MatchPopups` mobile.** |
| S27.3 | Mobile : i18n complet (FR/EN) + refactor `lobby.tsx` (816l) | AMELIO | L | [x] | `apps/mobile/app/settings.tsx:152` et autres : strings hardcoded en francais. Ajouter `i18next` mobile aligne avec le web. Splitter `lobby.tsx` en `MatchCard`, `MatchList`, `FilterBar`. **EN COURS : S27.3.1 (i18n foundation) — module leger sans dependance externe (`apps/mobile/lib/i18n.ts`) avec fonction pure `t(key, params?, lang?)` typee strictement via les cles de `FR_TRANSLATIONS`. Helpers `isLocale` / `resolveLocale('fr-FR' -> 'fr')`. Dictionnaires `translations/fr.ts` + `translations/en.ts` (namespace `common.*` initial). Fallback FR si cle EN absente, retour cle brute si rien (forward-compat). Interpolation `{{name}}`. 10 tests. S27.3.2 (hook React + settings.tsx) — helper pur `detectDeviceLocale(intl)` qui resout la locale device via `Intl.DateTimeFormat().resolvedOptions().locale` avec fallback FR (8 tests, supporte env sans Intl, throw, locale vide ou inconnue). Context `LocaleProvider` + hook `useTranslation()` (`apps/mobile/lib/i18n-context.tsx`) qui retourne `{ locale, setLocale, t }` avec `t` lie a la locale active. Helper testable `resolveInitialLocale` extrait (4 tests : initial explicite gagne / Intl injecte / fallback null / globalThis.Intl). `_layout.tsx` wrappe l'arbre avec `LocaleProvider` (entre GestureHandlerRootView et AuthProvider). Dictionnaire etendu avec namespace `settings.*` (profile/password/delete/actions). `settings.tsx` : 13 strings hardcoded remplacees (Profil introuvable, Erreur lors du chargement, Profil enregistre, Erreur lors de la sauvegarde, Mot de passe modifie avec succes, Erreur lors du changement de mot de passe, Erreur lors de la suppression du compte, Supprimer le compte, Etes-vous sur ?..., Annuler, Supprimer, Reessayer, Deconnexion). Reutilise `common.cancel` et `common.retry` pour eviter la duplication. 22 tests i18n totaux (10 + 8 + 4). S27.3.3 (sub-components settings/*) — extension des dictionnaires FR/EN avec 32 nouvelles cles reparties dans `settings.profile/security/danger/account/stats/profileHeader` (titres, boutons editButton/saveButton/changeButton, labels champs profil et password, description danger zone, infos compte, statistiques, badge Patreon). Migration de 5 sub-components vers `useTranslation()` : `ProfileEditSection.tsx` (titre Profil, boutons Modifier/Enregistrer/Annuler, 5 labels champs + placeholder), `PasswordChangeSection.tsx` (titre Securite, bouton Changer le mot de passe, 3 labels password fields), `DangerZone.tsx` (titre Suppression du compte, description, bouton Supprimer mon compte), `AccountInfoSection.tsx` + `StatsSection` (titres, 7 InfoRow labels, 4 StatCard labels), `ProfileHeader.tsx` (badge Patreon). 91 tests i18n ajoutes (32 cles x 3 verifications : non-vide FR, non-vide EN, FR != EN sur 27 cles requerant trad distincte). 412 tests mobile passent. S27.3.4 (refactor lobby.tsx + i18n complet) — extraction de `lobby.tsx` (816l -> 209l, -74%) en sous-modules : `lib/lobby/match-filter.ts` (helpers purs `filterMatches` + `countMyTurn` + types `MatchSummary`/`LobbyFilter`), `lib/lobby/match-display.ts` (helpers purs `getStatusLabel(status, t)` / `getStatusColor` / `formatMatchDate(date, locale)` / `formatRoundLabel(half, turn, t)`), composants `components/lobby/MatchCard.tsx` (101l), `MatchList.tsx` (57l), `FilterBar.tsx` (54l), `LobbyHeader.tsx` (59l), `LobbyActions.tsx` (90l), `MyTurnBanner.tsx` (27l), `JoinMatchModal.tsx` (83l), styles partages `components/lobby/lobby.styles.ts` (340l). Ajout namespace `lobby.*` dans dictionnaires FR/EN (40+ cles : title/greeting, status active/pending/prematch/prematchSetup/ended, myTurnBadge, myTurnBannerSingular/Plural, teamPlaceholder, waitingOpponent, vsCoach, roundLabel, filters all/myTurn/active/ended, actions teams/leaderboard/profile/matchmaking/cups/leagues/stars/create/join/replay/replayA11y, alerts matchCreated/createError/joinSuccess/joinError/loadError, errors prefix/empty, joinModal title/inputPlaceholder) + `common.logout`. 143 nouveaux tests (9 match-filter + 10 match-display + 124 i18n keys parite stricte FR != EN). 555 tests mobile passent. S27.3.5 (auth screens) — namespace `auth.*` (login/register) avec 11 cles dans dictionnaires FR/EN. Migration `apps/mobile/app/login.tsx` (8 strings : title/emailLabel/emailPlaceholder/passwordLabel/passwordPlaceholder/submit/submitError/preAlphaNotice) et `apps/mobile/app/register.tsx` (3 strings : title/message/backToLogin). Login : remplacement du fallback hardcode `"Erreur lors de la connexion"` par `t("auth.login.submitError")`, narrowing `unknown` propre (typage strict, plus d'`any`). 31 tests i18n auth ajoutes (11 FR non-vide + 11 EN non-vide + 9 cles FR != EN, "Email" et email d'exemple identiques par design). 586 tests mobile passent. S27.3.6 (matchmaking screen) — namespace `matchmaking.*` avec 14 cles dans dictionnaires FR/EN reparties en : `title`, `subtitle`, `searching` (title/teamValueRange/cancel), `empty` (title/description/createTeam), `form` (label/submit), `errors` (selectTeam/loadTeams/joinQueue/cancelSearch). Migration `apps/mobile/app/matchmaking.tsx` : 13 strings hardcoded remplacees, integration `useTranslation()`, fallback `validation.error` du helper `canStartSearch` remplace par `t("matchmaking.errors.selectTeam")` (dependance i18n centralisee dans le composant, le helper reste pur). `teamValueRange` interpole `{{value}}` (formatTVShort) et `{{range}}` (formatTVRange). 43 tests i18n ajoutes (14 FR non-vide + 14 EN non-vide + 14 FR != EN parite stricte + 1 test interpolation `teamValueRange` avec verification FR/EN). 629 tests mobile passent. S27.3.7 (leaderboard screen) — namespace `leaderboard.*` avec 10 cles dans dictionnaires FR/EN reparties en : `title`, `stats` (players/best/average), `empty`, `errors` (loadError/prefix), `pagination` (previous/next/pageOf). Migration `apps/mobile/app/leaderboard.tsx` : 10 strings hardcoded remplacees, integration `useTranslation()`. Reutilise `common.retry` pour le bouton de retry afin d'eviter la duplication. `pageOf` interpole `{{current}}` et `{{total}}`. `errors.prefix` interpole `{{message}}`. 31 tests i18n ajoutes (10 FR non-vide + 10 EN non-vide + 9 FR != EN parite stricte ; "Page X / Y" est legitimement identique FR/EN car le format reste universel + 1 test pageOf + 1 test errors.prefix). 660 tests mobile passent. S27.3.8 (teams list screen) — namespace `teams.list.*` avec 6 cles dans dictionnaires FR/EN reparties en : `title`, `createButton`, `empty`, `createFirst`, `errors` (loadError/prefix). Migration `apps/mobile/app/teams/index.tsx` : 7 strings hardcoded remplacees, integration `useTranslation()`. Reutilise `common.retry` pour le bouton de retry. `errors.prefix` interpole `{{message}}`. Le namespace est `teams.list.*` (et non `teams.*`) pour laisser la place a `teams.detail.*` et `teams.new.*` lors des slices suivantes. 19 tests i18n ajoutes (6 FR non-vide + 6 EN non-vide + 6 FR != EN parite stricte + 1 test interpolation prefix). 679 tests mobile passent. S27.3.9 (teams new screen) — namespace `teams.new.*` avec 14 cles dans dictionnaires FR/EN reparties en : `nameLabel`, `namePlaceholder`, `rosterLabel`, `rosterTier`, `budgetLabel`, `submit`, `errors` (loadError/prefix/invalidNameTitle/rosterRequiredTitle/rosterRequiredMessage/invalidBudgetTitle/createErrorTitle/createErrorMessage). Migration `apps/mobile/app/teams/new.tsx` : 13 strings hardcoded remplacees, integration `useTranslation()`. Reutilise `common.cancel` pour Annuler et `common.retry` pour Reessayer. `errors.prefix` interpole `{{message}}`, `rosterTier` interpole `{{tier}}`. Les keys `rosterLabel` ("Roster") et `rosterTier` ("Tier {{tier}}") sont legitimement identiques FR/EN (termes de domaine Blood Bowl). 42 tests i18n ajoutes (14 FR non-vide + 14 EN non-vide + 12 FR != EN parite stricte + 1 test interpolation prefix + 1 test interpolation rosterTier). 721 tests mobile passent. Restent : migrer les autres ecrans teams (`[id].tsx`), `play`, `replay` + audit script i18n. S27.3.10 (audit script i18n) — module pur `apps/mobile/lib/i18n-audit.ts` (~130l) avec helpers `findHardcodedStrings(content, filename)` et `summarizeAudits(audits)`. Heuristique double : (1) accents FR via regex `[À-ÿ]` (capture é, è, à, ç, etc.), (2) mots-cles FR courants (Profil, Erreur, Annuler, Veuillez, Mot de passe, Connexion, Equipe, Joueur, Match, Tour, Defaite, Victoire, Bienvenue, Chargement, Reessayer, Retour, Confirmer, Enregistrer, Modifier, Supprimer, Inscription, Deconnexion). Exclusions : chaines deja dans `t(...)` (walk back depuis le literal jusqu'a trouver `(` precede de `t` avec word boundary), lignes d'import/export/`from`, commentaires `//` et JSDoc `*`. CLI `apps/mobile/scripts/audit-i18n.ts` (~110l) qui scanne `app/` et `components/` recursivement (skip `.test.ts/tsx`, `.d.ts`, `node_modules`, fichiers cachés), imprime resume par fichier (count desc) + details (5 par fichier max sauf `--full`), exit 1 si findings, 0 sinon. Script npm `pnpm --filter @bb/mobile audit:i18n`. Baseline initiale : **42 chaines hardcodees dans 17 fichiers** (top : `play/[id]` 5, `player/[teamId]/[playerId]` 5, `BlockChoicePopup` 5, `leagues/[id]` 4, `teams/[id]` 4, `MatchPopups` 4, `_layout` 3). 20 nouveaux tests `lib/i18n-audit.test.ts` (accents detection, keyword detection, ignore EN/neutre, ignore t() / imports / commentaires / JSDoc, robustesse multi-lignes / source vide / apostrophes simples, summarize tri desc + agregation). 700+ tests mobile passent. S27.3.11 (teams detail screen) — namespace `teams.detail.*` avec 23 cles dans dictionnaires FR/EN reparties en : `notFound`, `backToList`, `backButton`, `stats` (value/treasury/players), `configuration` (title/rerolls/cheerleaders/assistants/dedicatedFans/apothecary/onLabel/offLabel/yes/no/editButton/saveButton), `players` (title avec `{{count}}`/empty), `starPlayers` (title avec `{{count}}`), `errors` (loadError/saveErrorMessage). Migration `apps/mobile/app/teams/[id].tsx` : 28 strings hardcoded remplacees (4 detectes par audit + 24 supplementaires non-accents/non-keywords : labels stats, counters, toggle OUI/NON, boutons, sections), integration `useTranslation()`. Reutilise `common.cancel` pour Annuler et `common.error` pour le titre d'alerte de sauvegarde. `players.title` et `starPlayers.title` interpolent `{{count}}`. Les keys `configuration.title` ("Configuration") et `starPlayers.title` ("Star Players ({{count}})") sont legitimement identiques FR/EN (termes de domaine Blood Bowl). 69 tests i18n ajoutes (23 FR non-vide + 23 EN non-vide + 21 FR != EN parite stricte + 1 test interpolation players + 1 test interpolation starPlayers). Audit baseline tombe a **38 chaines dans 16 fichiers** (-4 sur teams/[id], -1 fichier). 810 tests mobile passent. S27.3.12 (player detail screen) — namespace `players.detail.*` avec 36 cles dans dictionnaires FR/EN. Refactor des helpers purs `lib/player-details.ts` pour rendre le module locale-agnostic : `computePlayerStatus()` retourne desormais `PlayerStatusKind = "dead" | "miss-next-match" | "injured" | "fit"` (plus de strings FR), `computeInjurySummary()` retourne `InjurySummaryEntry[]` discriminant (`{kind:"niggling",count}` / `{kind:"stat-reduction",stat,value}` / `{kind:"miss-next-match"}`), suppression de `formatAdvancementType` et du champ `label` dans `AdvancementOption` (le formatage i18n migre en UI via `t()`). Nouveau module `lib/players-detail-keys.ts` (mappings `PLAYER_STATUS_TRANSLATION_KEYS` et `ADVANCEMENT_TRANSLATION_KEYS` typees `TranslationKey`, hors scope de l'audit i18n pour eviter les faux positifs sur le keyword "Match" capture dans la cle `players.detail.status.missNextMatch`). Migration `apps/mobile/app/player/[teamId]/[playerId].tsx` (~25 strings hardcoded remplacees : titres de sections, labels InfoRow, statut, types d'avancement, summary blessures, alertes "Equipe introuvable" / "Joueur introuvable" / "Erreur de chargement", bouton "Retour a l'equipe", message "Joueur decede", `subtitle` `{{position}} — {{team}}`, `statBase` `base {{value}}`), integration `useTranslation()` + helper local `formatInjuryEntry(entry, t)`. Tests `player-details.test.ts` adaptes (+1 test "no label leaked" sur `getNextAdvancementOptions`, refonte des tests `computeInjurySummary` et `computePlayerStatus` pour matcher la nouvelle API). Nouveau fichier `lib/i18n-keys-players-detail.test.ts` (103 tests : FR non-vide x36, EN non-vide x36 + cle != value, FR != EN strict x28, interpolations `{{count}}` niggling FR+EN, `{{stat}}` + `{{value}}` statReduction FR+EN). Audit baseline tombe a **33 chaines dans 15 fichiers** (-5, -1 fichier). 915 tests mobile passent. S27.3.13 (play/[id] screen) — namespace `play.*` avec 14 cles dans dictionnaires FR/EN reparties en : `loading`, `noState`, `hint`, `errors` (loadError/actionError/prefix), `banner` (myTurn/opponentTurn/offline), `actions` (throwTeamMate/cancelTarget/cancelThrower/endTurn/back). Migration `apps/mobile/app/play/[id].tsx` : 12 strings hardcodees remplacees, integration `useTranslation()`. Reutilise `common.cancel` pour le bouton de deselection joueur et `common.retry` pour le bouton de retry. `errors.prefix` interpole `{{message}}`. 44 tests i18n ajoutes (14 FR non-vide + 14 EN non-vide + 14 FR != EN parite stricte + 1 test interpolation prefix FR + 1 test interpolation prefix EN). Audit baseline tombe a **28 chaines dans 14 fichiers** (-5, -1 fichier). 959 tests mobile passent. S27.3.14 (popups in-match) — namespace `popups.*` avec 20 cles dans dictionnaires FR/EN reparties en : `fallbacks` (attacker/defender/player), `block` (title/chooserChoosing/chooseA11y/results.{playerDown,bothDown,pushBack,stumble,pow}), `push` (title/subtitle/pushTowardsA11y), `followUp` (title/subtitle/question/yes/no/hint). Migration des 4 popups in-match : `BlockChoicePopup.tsx` (5 strings : titre, chooser line, label "Choisir {{result}}", 5 results dans un mapping `RESULT_LABEL_KEYS` typee `TranslationKey`, bouton Annuler), `PushChoicePopup.tsx` (3 strings : titre, subtitle interpolant `{{attacker}}` et `{{target}}`, label "Pousser vers {{label}}", bouton Annuler), `FollowUpChoicePopup.tsx` (6 strings : titre, subtitle interpolant `{{attacker}}/{{target}}/{{x}}/{{y}}`, question, Suivre/Ne pas suivre, hint, bouton Annuler), `MatchPopups.tsx` (3 fallbacks : Attaquant/Defenseur/Joueur traduits via `t("popups.fallbacks.*")` et passes en props aux popups enfants). Reutilise `common.cancel` pour les boutons Annuler. La key `popups.followUp.title` ("Suivi (Follow-up)") reste identique FR/EN par design (terme du jeu) ; idem `popups.block.results.pow` ("POW !"/"POW!") quasi identique modulo l'espace insecable typographique FR. 64 tests i18n ajoutes (20 FR non-vide + 20 EN non-vide + 18 FR != EN parite stricte + 6 tests d'interpolation : `{{name}}` chooser FR+EN, `{{result}}` chooseA11y FR, `{{attacker}}/{{target}}` push subtitle FR, `{{attacker}}/{{target}}/{{x}}/{{y}}` followUp subtitle FR, `{{label}}` pushTowardsA11y EN). Audit baseline tombe a **17 chaines dans 10 fichiers** (-11, -4 fichiers). 1023 tests mobile passent. S27.3.15 (leagues screens) — namespace `leagues.*` avec 34 cles dans dictionnaires FR/EN reparties en : `public`/`private` (partage liste/detail), `list` (title/filters.all/empty/errors.{loadError,prefix}/card.maxParticipants), `detail` (notFound/creatorMeta, sections.{scoring,seasons,rounds,standings,participants}, scoring.{win,draw,loss,forfeit}, seasons.{empty,tabLabel}, rounds.{empty,label,labelWithName}, standings.{empty,headers.{team,wins,draws,losses,points}}, participants.{empty,summary}, errors.{loadError,prefix}). Migration `apps/mobile/app/leagues/index.tsx` : 7 strings hardcodees remplacees + helper local `buildFilterOptions(t)` pour eviter de recalculer les labels statiquement (premier filtre `all` traduit, statuts conserves via `formatLeagueStatusLabel`), `LeagueCard` enfant recoit `t` en prop pour rester un composant pur. Migration `apps/mobile/app/leagues/[id].tsx` : 25+ strings hardcodees remplacees (badge Publique/Privee, meta createur interpolant `{{creator}}` et `{{count}}`, titres sections, scoring labels, season tab `S{{number}} — {{name}}`, journee labels avec/sans nom, headers V/N/D/Pts, summary participant `{{roster}} • {{coach}} • ELO {{elo}}`, erreurs sans/avec prefix). Reutilise `common.retry` pour les boutons Reessayer. Les keys `participants` (sections+scoring) "Participants" et `standings.headers.{wins,draws,losses,points}` (V/N/D/Pts) sont legitimement identiques ou tres proches FR/EN (abreviations universelles de tableau de classement, "Pts" et terme "Participants" inchanges). 102 tests i18n ajoutes (34 FR non-vide + 34 EN non-vide + 27 FR != EN parite stricte + 7 interpolations : `{{message}}` prefix list FR + detail EN, `{{count}}` maxParticipants FR, `{{number}}+{{name}}` tabLabel FR + labelWithName FR, `{{creator}}+{{count}}` creatorMeta EN, `{{roster}}+{{coach}}+{{elo}}` summary FR). Audit baseline tombe a **12 chaines dans 8 fichiers** (-5, -2 fichiers). 1125 tests mobile passent. S27.3.16 (cups screens) — namespace `cups.*` avec 31 cles dans dictionnaires FR/EN reparties en : `public`/`private` (partage liste/archived/detail), `list` (title/archivedLink/filters.all/empty/errors.{loadError,prefix}/card.{participantSingular,participantPlural}), `archived` (title/empty/errors.{loadError,prefix}), `detail` (notFound/creatorMeta, sections.{participants,standings,matches}, participants.{empty,summary}, standings.{empty,headers.{team,wins,draws,losses,points}}, matches.{empty,label}, errors.{loadError,prefix}). Migration `apps/mobile/app/cups/index.tsx` : 8 strings hardcodees remplacees + helper local `buildFilterOptions(t)` (idem leagues) + `CupCard` enfant recoit `t` en prop. Migration `apps/mobile/app/cups/archived.tsx` : 4 strings hardcodees remplacees. Migration `apps/mobile/app/cups/[id].tsx` : 16 strings hardcodees remplacees (badge Publique/Privee, creatorMeta `Cree par {{creator}}`, sections Participants/Classement/Matchs, participants summary `{{roster}} — {{coach}}`, standings headers V/N/D/Pts, label match `Match {{id}}`, mutedText, erreurs). Reutilise `common.retry` pour les boutons Reessayer. Les keys `participantSingular/Plural` ("{{count}} participant(s)") et `matches.label` ("Match {{id}}") sont legitimement identiques FR/EN (mot "participant" identique en francais, "Match" terme universel) ; idem standings.headers V/N/D/Pts (abbreviations partagees). 92 tests i18n ajoutes (31 FR non-vide + 31 EN non-vide + 22 FR != EN parite stricte + 8 interpolations : `{{message}}` prefix list/archived/detail, `{{count}}` participantSingular/Plural FR, `{{creator}}` creatorMeta EN, `{{roster}}+{{coach}}` summary FR, `{{id}}` matches.label FR). Audit baseline tombe a **8 chaines dans 5 fichiers** (-4, -3 fichiers). 1217 tests mobile passent. S27.3.17 (star-players screens) — namespace `starPlayers.*` avec 24 cles dans dictionnaires FR/EN reparties en : `megaStarBadge` (partage liste/detail), `list` (title/subtitleSingular/Plural/Filtered/search.placeholder/ruleset.{season3,season2}/megaToggle.{on,off}/empty/row.hirable/errors.{loadError,prefix}), `detail` (notFound/imageA11y, sections.{stats,skills,hirable,specialRule}, skills.empty, actions.{back,backToCatalog}, errors.loadError). Migration `apps/mobile/app/star-players/index.tsx` : 12 strings hardcodees remplacees + RULESET_OPTIONS desormais reference des `TranslationKey` (resolution via `t(opt.labelKey)` au render), `StarRow` enfant recoit `t` en prop pour rester pur. Migration `apps/mobile/app/star-players/[slug].tsx` : 11 strings hardcodees remplacees (notFound, image a11y interpolant `{{name}}`, sections, skills empty, hirable, regle speciale, retour boutons, badge Mega Star). Reutilise `common.retry` pour les boutons Reessayer. La key `megaToggle.off` ("Mega stars ({{count}})") est legitimement identique FR/EN (terme universel) ; idem `megaStarBadge` ("Mega Star") conserve dans les deux langues. 77 tests i18n ajoutes (24 FR non-vide + 24 EN non-vide + 22 FR != EN parite stricte + 7 interpolations : `{{count}}` subtitleSingular FR + subtitlePlural EN + megaToggle.off FR, `{{total}}` subtitleFiltered FR, `{{hirable}}` row FR, `{{message}}` prefix EN, `{{name}}` imageA11y FR). Audit baseline tombe a **6 chaines dans 3 fichiers** (-2, -2 fichiers). 1294 tests mobile passent. S27.3.18 (match + replay screens) — namespaces `match.*` et `replay.*` avec 43 cles dans dictionnaires FR/EN. `match.history.*` (29 cles) couvre titre/back/empty/scoreFinal interpole `{{teamA}}+{{teamB}}`/stats.{actions,movesPlayed}/errors.{loadError,prefix}/turnRound interpole `{{half}}+{{turn}}`/actionFallback + 8 turnTypes (start/accept/coinToss/selectKickTeam/validateSetup/kickoffSequence/kickoffScatter/kickoffEventResolved) + 11 moveTypes (move/block/blitz/pass/handoff/foul/endTurn/select/chooseBlockResult/choosePushDirection/followUp). `replay.*` (14 cles) couvre loading/empty/back/errors.loadError/halfTurn interpole `{{half}}+{{turn}}`/transport.{start,previous,next,end,play,pause}/speed.{label,a11y interpole `{{label}}`}/actionLabel interpole `{{label}}`. Migration `apps/mobile/app/match/[id].tsx` : helpers locale-agnostic `getTurnTypeLabelKey()` et `getMoveLabelKey()` retournent une `TranslationKey | null` (refactor depuis les anciennes fonctions retournant des strings FR), nouveau helper `resolveTurnLabel(type, moveType, t)` qui appelle `t()` avec fallback `match.history.actionFallback`. `TurnCard` recoit `t` en prop pour rester pur. 8 strings hardcodees remplacees dans la UI (titre, back, score final, stats, vide, erreurs, MT/Tour). Migration `apps/mobile/app/replay/[id].tsx` : 13 strings hardcodees remplacees (loading, errors fallback, retour boutons, mi-temps/tour, transport a11y x6, vitesse label + a11y interpolee, action label). Reutilise `common.error` pour le titre d'erreur et `common.retry` pour le bouton de retry. Les keys "Blitz", "Selection" et "Pause" sont legitimement identiques FR/EN (termes universels). 132 tests i18n ajoutes (43 FR non-vide + 43 EN non-vide + 40 FR != EN parite stricte + 6 interpolations : `{{teamA}}+{{teamB}}` scoreFinal FR, `{{half}}+{{turn}}` turnRound EN + halfTurn FR, `{{message}}` prefix FR, `{{label}}` speed.a11y EN + actionLabel FR). Audit baseline tombe a **3 chaines dans 1 fichier** (-3, -2 fichiers). 1426 tests mobile passent. S27.3.19 (cloture S27.3 : `_layout.tsx`) — namespace `nav.*` avec 19 cles dans dictionnaires FR/EN couvrant les 19 titres de Stack.Screen (appName/lobby/matchmaking/leaderboard/login/register/matchHistory/replay/teamsList/teamsNew/teamsDetail/cupsList/cupsArchived/cupsDetail/leaguesList/leaguesDetail/starPlayersList/starPlayersDetail/settings). Refactor `_layout.tsx` : extraction d'un sous-composant `AppStack` interne au `LocaleProvider` qui consomme `useTranslation()` et resout chaque titre via `t("nav.*")`. Le composant `Layout` reste l'enveloppe externe (GestureHandlerRootView -> LocaleProvider -> AuthProvider -> PushRegistration -> AppStack), garantissant que `t` est dispo au moment du rendu des Screens. Les keys "Nuffle Arena" (nom propre), "Star Players"/"Star Player" (termes Blood Bowl) et "Replay" (terme universel) sont legitimement identiques FR/EN. 53 tests i18n ajoutes (19 FR non-vide + 19 EN non-vide + 15 FR != EN parite stricte). **Audit baseline tombe a 0 chaine hardcodee dans 0 fichier (-3, -1 fichier) — objectif "Mobile : 100% des strings en i18n" atteint.** 1479 tests mobile passent.** |
| S27.4 | Mobile : accessibilite touch board + hooks composites | AMELIO | M | [~] | `apps/mobile/app/play/[id].tsx:363-367`. Ajouter `accessibilityRole`, `accessibilityLabel` sur PixiBoardNative. Consolider `use-game-socket` + `use-game-chat` -> `useGameMatch`. **EN COURS : S27.4.1 (a11y board) — helper pur `summarizeBoardForA11y(state)` (`apps/mobile/lib/board-a11y.ts`) qui extrait `{scoreA, scoreB, half, turn, currentTeam, teamA, teamB, playersA, playersB}` depuis `GameState` (compte les joueurs `on-field` ou sans `state` defini). Ajout namespace `play.a11y.*` (boardLabel interpolee + boardHint) dans dictionnaires FR/EN. PixiBoardNative accepte deux nouveaux props optionnels `accessibilityLabel` et `accessibilityHint` ; le `View` racine utilise `accessibilityRole="adjustable"` (zoom/pan + tap), `accessible`, et expose label/hint au screen reader. `play/[id].tsx` injecte les valeurs localisees via `t("play.a11y.boardLabel", summarizeBoardForA11y(state))` + `t("play.a11y.boardHint")`. 5 tests `board-a11y.test.ts` (score/half/turn, current team + names, comptage on-field vs reserves/casualties, fallback sans state, fallbacks score/half/teamNames absents) + 4 nouveaux tests `i18n-keys-play.test.ts` (boardLabel + boardHint en FR + EN, interpolation FR + EN). 1492 tests mobile passent. Reste : S27.4.2 (hooks composites `useGameMatch` consolidant `use-game-socket` + `use-game-chat`).** |
| S27.5 | Skeleton Season 4 BB3 (pattern S3 = clone + overrides) | EVO | M | [ ] | `packages/game-engine/src/rosters/star-players.ts:1105-1141`. Pattern S3 clair (`SEASON_THREE_STAR_PLAYER_OVERRIDES` + `buildSeason3StarPlayers()`). Creer `SEASON_FOUR_*` skeleton vide pret a recevoir les rosters officiels GW 2025-2026. Zero contenu mais infra prete. |
| S27.6 | Audit log admin (table AuditLog : qui change quoi sur skills/rosters/users) | EVO | L | [ ] | Aucune trace actuelle des actions admin (create/update/delete contenu). Compliance + tracabilite. Modele Prisma `AuditLog(userId, action, entity, entityId, oldValue Json?, newValue Json?, createdAt)`. Hook dans toutes les routes `/admin/data/*`. UI lecture `/admin/audit-log`. |
| S27.7 | B0.1 residuels : Stunty AV, GFI Sure Feet/Sprint, Horns vers registry | CONFORT | M | [ ] | Solde [`docs/roadmap/follow-up-b01.md`](../follow-up-b01.md). Etendre `SkillContext.currentTrigger` pour discriminer Stunty (on-dodge ET on-armor). Refactor `blocking.ts:141`, `movement.ts` (aucun import skill-bridge), `foul.ts` (Sneaky Git mixte). |
| S27.8 | Refactor `actions.ts` (2695l) + `match.ts` (1605l) + `team.ts` (2150l) en micro-modules | CONFORT | XL | [ ] | Etalable post-S27 si trop dense. `actions/{move,block,pass,foul,blitz,handoff}.ts` + `index.ts` unificateur. `match.ts` -> `match/{game,state,actions,prematch}.ts`. Pre-requis avant de poser de nouvelles features sur ces fichiers. |

## Definition of done

- [ ] 1 Nuffle Cup mensuelle ouverte, calendrier publie
- [ ] Mobile : Throw Team-Mate jouable de bout en bout
- [x] Mobile : 100% des strings en i18n (audit script)
- [ ] Schema Prisma `Season4*` present mais vide (migration applicable)
- [ ] AuditLog : 1 action admin = 1 entree visible dans `/admin/audit-log`
- [ ] Hardcode `player.skills.includes(...)` = 0 dans mechanics/blocking, movement, foul
- [ ] `actions.ts` <= 600 lignes, le reste en modules <= 400 lignes

## Risques

- **Esport Nuffle Cup (S27.1)** : risque de cup vide si pas assez de
  joueurs. Lancer en mode "soft" avec annonce Discord 1 semaine avant.
- **i18n mobile (S27.3)** : ne pas oublier les strings dans les
  components Reanimated/Pixi.js qui sont moins evidents a auditer.
- **Audit log (S27.6)** : risque de saturer la DB si log trop verbeux.
  Limiter aux actions /admin/data/* + /admin/users (pas les lectures).
- **Refactor monoliths (S27.8)** : risque XL d'eclater le scope.
  Decoupage incremental, 1 fichier par PR, tests e2e en garde-fou.

## Sources

Findings agents : engagement#6 (esport), mobile#1-6 (parite + i18n + lobby
+ a11y), contenu#5 (S4 skeleton), securite#6 (audit log), engine#2,
follow-up-b01.md (B0.1 residuels), engine#5 + backend#4 (refactor monoliths).

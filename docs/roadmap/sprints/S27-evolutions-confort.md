# Sprint 27 — Evolutions & confort

> Duree cible : ~6 jours (allegé : replay sharable retiré, hot-reload
> Docker remonte en S24.9)
> Theme : preparer la suite long-terme et boucler la dette mineure.
> Pre-requis : S24 (DX hot-reload), S25 (logs/perf), S26 (refactor +
> retention).

## Taches

| # | Tache | Cat | Effort | Statut | Detail |
|---|-------|-----|--------|--------|--------|
| S27.1 | Compétitions esport mensuelles "Nuffle Cup" + bracket visuel + match-of-week | EVO | L | [x] | **TERMINE en S27.1a-j.** S27.1a (foundation) — helpers `nuffle-cup-monthly.ts` + champs `monthlyYear`/`monthlyMonth` sur Cup + migration. S27.1b (endpoint listing) — service `cup-monthly-listing.ts` + route `GET /cup/monthly`. S27.1c (UI calendrier) — page publique `/cups/monthly`. S27.1d (badge champion backend) — service `cup-championships.ts` + `cupChampionships` dans `GET /coach/:slug`. S27.1e (badge UI) — `CoachCupChampionshipsBanner.tsx` sur `/coach/[slug]`. S27.1f (match-of-the-week lecture) — champs `featuredAt`/`featuredNote` sur LocalMatch + service `match-of-the-week.ts` (`getCurrentMatchOfTheWeek()`) + route `GET /cup/match-of-the-week`. S27.1g (admin pick) — `setMatchOfTheWeek({matchId, note})` defensif + route `POST /cup/match-of-the-week/:matchId` (admin only). S27.1h (UI banner match-of-the-week) — `MatchOfTheWeekBanner.tsx` sur `/cups/monthly`. S27.1i (creation cup mensuelle admin) — `createCupSchema` etendu + handler `POST /cup` propage si admin sinon 403. S27.1j (bracket visuel) — composant `apps/web/app/cups/[id]/CupBracketView.tsx` (vue chronologique des matches, mise en evidence vainqueur via `data-winner='A'|'B'|'draw'`, masque score si pending), branche additivement dans `/cups/[id]/page.tsx`. 7 nouveaux tests UI. |
| S27.2 | Mobile parite : Action Picker Throw Team-Mate + flow 2-clics | AMELIO | M | [x] | `apps/mobile/app/play/[id].tsx`. Refrappe ce qui a ete fait en S23 sur web (commit `28c31e4`). Backend deja pret (B2.4). **TERMINE : S27.2.1 helper pur `processThrowTeamMateClick` (4 etats discriminants, 6 tests). S27.2.2 helper `canActivateThrowTeamMate` + integration page : state `currentAction` + `throwTeamMateThrownId`, branche `processThrowTeamMateClick` au debut de `onCellClick` (priorite sur MOVE/BLOCK), bouton "Lancer un coequipier" dans actionsBar (apparait si selected player a au moins 1 legal `THROW_TEAM_MATE` et flow inactif), bouton "Annuler (lancer/cible)" pendant le flow (label change selon Phase 1/2). 4 tests `canActivateThrowTeamMate`. Total 10 tests. Les autres popups (block/push/follow-up/reroll) etaient deja parite par `MatchPopups` mobile.** |
| S27.3 | Mobile : i18n complet (FR/EN) + refactor `lobby.tsx` (816l) | AMELIO | L | [x] | `apps/mobile/app/settings.tsx:152` et autres : strings hardcoded en francais. Ajouter `i18next` mobile aligne avec le web. Splitter `lobby.tsx` en `MatchCard`, `MatchList`, `FilterBar`. **EN COURS : S27.3.1 (i18n foundation) — module leger sans dependance externe (`apps/mobile/lib/i18n.ts`) avec fonction pure `t(key, params?, lang?)` typee strictement via les cles de `FR_TRANSLATIONS`. Helpers `isLocale` / `resolveLocale('fr-FR' -> 'fr')`. Dictionnaires `translations/fr.ts` + `translations/en.ts` (namespace `common.*` initial). Fallback FR si cle EN absente, retour cle brute si rien (forward-compat). Interpolation `{{name}}`. 10 tests. S27.3.2 (hook React + settings.tsx) — helper pur `detectDeviceLocale(intl)` qui resout la locale device via `Intl.DateTimeFormat().resolvedOptions().locale` avec fallback FR (8 tests, supporte env sans Intl, throw, locale vide ou inconnue). Context `LocaleProvider` + hook `useTranslation()` (`apps/mobile/lib/i18n-context.tsx`) qui retourne `{ locale, setLocale, t }` avec `t` lie a la locale active. Helper testable `resolveInitialLocale` extrait (4 tests : initial explicite gagne / Intl injecte / fallback null / globalThis.Intl). `_layout.tsx` wrappe l'arbre avec `LocaleProvider` (entre GestureHandlerRootView et AuthProvider). Dictionnaire etendu avec namespace `settings.*` (profile/password/delete/actions). `settings.tsx` : 13 strings hardcoded remplacees (Profil introuvable, Erreur lors du chargement, Profil enregistre, Erreur lors de la sauvegarde, Mot de passe modifie avec succes, Erreur lors du changement de mot de passe, Erreur lors de la suppression du compte, Supprimer le compte, Etes-vous sur ?..., Annuler, Supprimer, Reessayer, Deconnexion). Reutilise `common.cancel` et `common.retry` pour eviter la duplication. 22 tests i18n totaux (10 + 8 + 4). S27.3.3 (sub-components settings/*) — extension des dictionnaires FR/EN avec 32 nouvelles cles reparties dans `settings.profile/security/danger/account/stats/profileHeader` (titres, boutons editButton/saveButton/changeButton, labels champs profil et password, description danger zone, infos compte, statistiques, badge Patreon). Migration de 5 sub-components vers `useTranslation()` : `ProfileEditSection.tsx` (titre Profil, boutons Modifier/Enregistrer/Annuler, 5 labels champs + placeholder), `PasswordChangeSection.tsx` (titre Securite, bouton Changer le mot de passe, 3 labels password fields), `DangerZone.tsx` (titre Suppression du compte, description, bouton Supprimer mon compte), `AccountInfoSection.tsx` + `StatsSection` (titres, 7 InfoRow labels, 4 StatCard labels), `ProfileHeader.tsx` (badge Patreon). 91 tests i18n ajoutes (32 cles x 3 verifications : non-vide FR, non-vide EN, FR != EN sur 27 cles requerant trad distincte). 412 tests mobile passent. S27.3.4 (refactor lobby.tsx + i18n complet) — extraction de `lobby.tsx` (816l -> 209l, -74%) en sous-modules : `lib/lobby/match-filter.ts` (helpers purs `filterMatches` + `countMyTurn` + types `MatchSummary`/`LobbyFilter`), `lib/lobby/match-display.ts` (helpers purs `getStatusLabel(status, t)` / `getStatusColor` / `formatMatchDate(date, locale)` / `formatRoundLabel(half, turn, t)`), composants `components/lobby/MatchCard.tsx` (101l), `MatchList.tsx` (57l), `FilterBar.tsx` (54l), `LobbyHeader.tsx` (59l), `LobbyActions.tsx` (90l), `MyTurnBanner.tsx` (27l), `JoinMatchModal.tsx` (83l), styles partages `components/lobby/lobby.styles.ts` (340l). Ajout namespace `lobby.*` dans dictionnaires FR/EN (40+ cles : title/greeting, status active/pending/prematch/prematchSetup/ended, myTurnBadge, myTurnBannerSingular/Plural, teamPlaceholder, waitingOpponent, vsCoach, roundLabel, filters all/myTurn/active/ended, actions teams/leaderboard/profile/matchmaking/cups/leagues/stars/create/join/replay/replayA11y, alerts matchCreated/createError/joinSuccess/joinError/loadError, errors prefix/empty, joinModal title/inputPlaceholder) + `common.logout`. 143 nouveaux tests (9 match-filter + 10 match-display + 124 i18n keys parite stricte FR != EN). 555 tests mobile passent. S27.3.5 (auth screens) — namespace `auth.*` (login/register) avec 11 cles dans dictionnaires FR/EN. Migration `apps/mobile/app/login.tsx` (8 strings : title/emailLabel/emailPlaceholder/passwordLabel/passwordPlaceholder/submit/submitError/preAlphaNotice) et `apps/mobile/app/register.tsx` (3 strings : title/message/backToLogin). Login : remplacement du fallback hardcode `"Erreur lors de la connexion"` par `t("auth.login.submitError")`, narrowing `unknown` propre (typage strict, plus d'`any`). 31 tests i18n auth ajoutes (11 FR non-vide + 11 EN non-vide + 9 cles FR != EN, "Email" et email d'exemple identiques par design). 586 tests mobile passent. S27.3.6 (matchmaking screen) — namespace `matchmaking.*` avec 14 cles dans dictionnaires FR/EN reparties en : `title`, `subtitle`, `searching` (title/teamValueRange/cancel), `empty` (title/description/createTeam), `form` (label/submit), `errors` (selectTeam/loadTeams/joinQueue/cancelSearch). Migration `apps/mobile/app/matchmaking.tsx` : 13 strings hardcoded remplacees, integration `useTranslation()`, fallback `validation.error` du helper `canStartSearch` remplace par `t("matchmaking.errors.selectTeam")` (dependance i18n centralisee dans le composant, le helper reste pur). `teamValueRange` interpole `{{value}}` (formatTVShort) et `{{range}}` (formatTVRange). 43 tests i18n ajoutes (14 FR non-vide + 14 EN non-vide + 14 FR != EN parite stricte + 1 test interpolation `teamValueRange` avec verification FR/EN). 629 tests mobile passent. S27.3.7 (leaderboard screen) — namespace `leaderboard.*` avec 10 cles dans dictionnaires FR/EN reparties en : `title`, `stats` (players/best/average), `empty`, `errors` (loadError/prefix), `pagination` (previous/next/pageOf). Migration `apps/mobile/app/leaderboard.tsx` : 10 strings hardcoded remplacees, integration `useTranslation()`. Reutilise `common.retry` pour le bouton de retry afin d'eviter la duplication. `pageOf` interpole `{{current}}` et `{{total}}`. `errors.prefix` interpole `{{message}}`. 31 tests i18n ajoutes (10 FR non-vide + 10 EN non-vide + 9 FR != EN parite stricte ; "Page X / Y" est legitimement identique FR/EN car le format reste universel + 1 test pageOf + 1 test errors.prefix). 660 tests mobile passent. S27.3.8 (teams list screen) — namespace `teams.list.*` avec 6 cles dans dictionnaires FR/EN reparties en : `title`, `createButton`, `empty`, `createFirst`, `errors` (loadError/prefix). Migration `apps/mobile/app/teams/index.tsx` : 7 strings hardcoded remplacees, integration `useTranslation()`. Reutilise `common.retry` pour le bouton de retry. `errors.prefix` interpole `{{message}}`. Le namespace est `teams.list.*` (et non `teams.*`) pour laisser la place a `teams.detail.*` et `teams.new.*` lors des slices suivantes. 19 tests i18n ajoutes (6 FR non-vide + 6 EN non-vide + 6 FR != EN parite stricte + 1 test interpolation prefix). 679 tests mobile passent. S27.3.9 (teams new screen) — namespace `teams.new.*` avec 14 cles dans dictionnaires FR/EN reparties en : `nameLabel`, `namePlaceholder`, `rosterLabel`, `rosterTier`, `budgetLabel`, `submit`, `errors` (loadError/prefix/invalidNameTitle/rosterRequiredTitle/rosterRequiredMessage/invalidBudgetTitle/createErrorTitle/createErrorMessage). Migration `apps/mobile/app/teams/new.tsx` : 13 strings hardcoded remplacees, integration `useTranslation()`. Reutilise `common.cancel` pour Annuler et `common.retry` pour Reessayer. `errors.prefix` interpole `{{message}}`, `rosterTier` interpole `{{tier}}`. Les keys `rosterLabel` ("Roster") et `rosterTier` ("Tier {{tier}}") sont legitimement identiques FR/EN (termes de domaine Blood Bowl). 42 tests i18n ajoutes (14 FR non-vide + 14 EN non-vide + 12 FR != EN parite stricte + 1 test interpolation prefix + 1 test interpolation rosterTier). 721 tests mobile passent. Restent : migrer les autres ecrans teams (`[id].tsx`), `play`, `replay` + audit script i18n. S27.3.10 (audit script i18n) — module pur `apps/mobile/lib/i18n-audit.ts` (~130l) avec helpers `findHardcodedStrings(content, filename)` et `summarizeAudits(audits)`. Heuristique double : (1) accents FR via regex `[À-ÿ]` (capture é, è, à, ç, etc.), (2) mots-cles FR courants (Profil, Erreur, Annuler, Veuillez, Mot de passe, Connexion, Equipe, Joueur, Match, Tour, Defaite, Victoire, Bienvenue, Chargement, Reessayer, Retour, Confirmer, Enregistrer, Modifier, Supprimer, Inscription, Deconnexion). Exclusions : chaines deja dans `t(...)` (walk back depuis le literal jusqu'a trouver `(` precede de `t` avec word boundary), lignes d'import/export/`from`, commentaires `//` et JSDoc `*`. CLI `apps/mobile/scripts/audit-i18n.ts` (~110l) qui scanne `app/` et `components/` recursivement (skip `.test.ts/tsx`, `.d.ts`, `node_modules`, fichiers cachés), imprime resume par fichier (count desc) + details (5 par fichier max sauf `--full`), exit 1 si findings, 0 sinon. Script npm `pnpm --filter @bb/mobile audit:i18n`. Baseline initiale : **42 chaines hardcodees dans 17 fichiers** (top : `play/[id]` 5, `player/[teamId]/[playerId]` 5, `BlockChoicePopup` 5, `leagues/[id]` 4, `teams/[id]` 4, `MatchPopups` 4, `_layout` 3). 20 nouveaux tests `lib/i18n-audit.test.ts` (accents detection, keyword detection, ignore EN/neutre, ignore t() / imports / commentaires / JSDoc, robustesse multi-lignes / source vide / apostrophes simples, summarize tri desc + agregation). 700+ tests mobile passent. S27.3.11 (teams detail screen) — namespace `teams.detail.*` avec 23 cles dans dictionnaires FR/EN reparties en : `notFound`, `backToList`, `backButton`, `stats` (value/treasury/players), `configuration` (title/rerolls/cheerleaders/assistants/dedicatedFans/apothecary/onLabel/offLabel/yes/no/editButton/saveButton), `players` (title avec `{{count}}`/empty), `starPlayers` (title avec `{{count}}`), `errors` (loadError/saveErrorMessage). Migration `apps/mobile/app/teams/[id].tsx` : 28 strings hardcoded remplacees (4 detectes par audit + 24 supplementaires non-accents/non-keywords : labels stats, counters, toggle OUI/NON, boutons, sections), integration `useTranslation()`. Reutilise `common.cancel` pour Annuler et `common.error` pour le titre d'alerte de sauvegarde. `players.title` et `starPlayers.title` interpolent `{{count}}`. Les keys `configuration.title` ("Configuration") et `starPlayers.title` ("Star Players ({{count}})") sont legitimement identiques FR/EN (termes de domaine Blood Bowl). 69 tests i18n ajoutes (23 FR non-vide + 23 EN non-vide + 21 FR != EN parite stricte + 1 test interpolation players + 1 test interpolation starPlayers). Audit baseline tombe a **38 chaines dans 16 fichiers** (-4 sur teams/[id], -1 fichier). 810 tests mobile passent. S27.3.12 (player detail screen) — namespace `players.detail.*` avec 36 cles dans dictionnaires FR/EN. Refactor des helpers purs `lib/player-details.ts` pour rendre le module locale-agnostic : `computePlayerStatus()` retourne desormais `PlayerStatusKind = "dead" | "miss-next-match" | "injured" | "fit"` (plus de strings FR), `computeInjurySummary()` retourne `InjurySummaryEntry[]` discriminant (`{kind:"niggling",count}` / `{kind:"stat-reduction",stat,value}` / `{kind:"miss-next-match"}`), suppression de `formatAdvancementType` et du champ `label` dans `AdvancementOption` (le formatage i18n migre en UI via `t()`). Nouveau module `lib/players-detail-keys.ts` (mappings `PLAYER_STATUS_TRANSLATION_KEYS` et `ADVANCEMENT_TRANSLATION_KEYS` typees `TranslationKey`, hors scope de l'audit i18n pour eviter les faux positifs sur le keyword "Match" capture dans la cle `players.detail.status.missNextMatch`). Migration `apps/mobile/app/player/[teamId]/[playerId].tsx` (~25 strings hardcoded remplacees : titres de sections, labels InfoRow, statut, types d'avancement, summary blessures, alertes "Equipe introuvable" / "Joueur introuvable" / "Erreur de chargement", bouton "Retour a l'equipe", message "Joueur decede", `subtitle` `{{position}} — {{team}}`, `statBase` `base {{value}}`), integration `useTranslation()` + helper local `formatInjuryEntry(entry, t)`. Tests `player-details.test.ts` adaptes (+1 test "no label leaked" sur `getNextAdvancementOptions`, refonte des tests `computeInjurySummary` et `computePlayerStatus` pour matcher la nouvelle API). Nouveau fichier `lib/i18n-keys-players-detail.test.ts` (103 tests : FR non-vide x36, EN non-vide x36 + cle != value, FR != EN strict x28, interpolations `{{count}}` niggling FR+EN, `{{stat}}` + `{{value}}` statReduction FR+EN). Audit baseline tombe a **33 chaines dans 15 fichiers** (-5, -1 fichier). 915 tests mobile passent. S27.3.13 (play/[id] screen) — namespace `play.*` avec 14 cles dans dictionnaires FR/EN reparties en : `loading`, `noState`, `hint`, `errors` (loadError/actionError/prefix), `banner` (myTurn/opponentTurn/offline), `actions` (throwTeamMate/cancelTarget/cancelThrower/endTurn/back). Migration `apps/mobile/app/play/[id].tsx` : 12 strings hardcodees remplacees, integration `useTranslation()`. Reutilise `common.cancel` pour le bouton de deselection joueur et `common.retry` pour le bouton de retry. `errors.prefix` interpole `{{message}}`. 44 tests i18n ajoutes (14 FR non-vide + 14 EN non-vide + 14 FR != EN parite stricte + 1 test interpolation prefix FR + 1 test interpolation prefix EN). Audit baseline tombe a **28 chaines dans 14 fichiers** (-5, -1 fichier). 959 tests mobile passent. S27.3.14 (popups in-match) — namespace `popups.*` avec 20 cles dans dictionnaires FR/EN reparties en : `fallbacks` (attacker/defender/player), `block` (title/chooserChoosing/chooseA11y/results.{playerDown,bothDown,pushBack,stumble,pow}), `push` (title/subtitle/pushTowardsA11y), `followUp` (title/subtitle/question/yes/no/hint). Migration des 4 popups in-match : `BlockChoicePopup.tsx` (5 strings : titre, chooser line, label "Choisir {{result}}", 5 results dans un mapping `RESULT_LABEL_KEYS` typee `TranslationKey`, bouton Annuler), `PushChoicePopup.tsx` (3 strings : titre, subtitle interpolant `{{attacker}}` et `{{target}}`, label "Pousser vers {{label}}", bouton Annuler), `FollowUpChoicePopup.tsx` (6 strings : titre, subtitle interpolant `{{attacker}}/{{target}}/{{x}}/{{y}}`, question, Suivre/Ne pas suivre, hint, bouton Annuler), `MatchPopups.tsx` (3 fallbacks : Attaquant/Defenseur/Joueur traduits via `t("popups.fallbacks.*")` et passes en props aux popups enfants). Reutilise `common.cancel` pour les boutons Annuler. La key `popups.followUp.title` ("Suivi (Follow-up)") reste identique FR/EN par design (terme du jeu) ; idem `popups.block.results.pow` ("POW !"/"POW!") quasi identique modulo l'espace insecable typographique FR. 64 tests i18n ajoutes (20 FR non-vide + 20 EN non-vide + 18 FR != EN parite stricte + 6 tests d'interpolation : `{{name}}` chooser FR+EN, `{{result}}` chooseA11y FR, `{{attacker}}/{{target}}` push subtitle FR, `{{attacker}}/{{target}}/{{x}}/{{y}}` followUp subtitle FR, `{{label}}` pushTowardsA11y EN). Audit baseline tombe a **17 chaines dans 10 fichiers** (-11, -4 fichiers). 1023 tests mobile passent. S27.3.15 (leagues screens) — namespace `leagues.*` avec 34 cles dans dictionnaires FR/EN reparties en : `public`/`private` (partage liste/detail), `list` (title/filters.all/empty/errors.{loadError,prefix}/card.maxParticipants), `detail` (notFound/creatorMeta, sections.{scoring,seasons,rounds,standings,participants}, scoring.{win,draw,loss,forfeit}, seasons.{empty,tabLabel}, rounds.{empty,label,labelWithName}, standings.{empty,headers.{team,wins,draws,losses,points}}, participants.{empty,summary}, errors.{loadError,prefix}). Migration `apps/mobile/app/leagues/index.tsx` : 7 strings hardcodees remplacees + helper local `buildFilterOptions(t)` pour eviter de recalculer les labels statiquement (premier filtre `all` traduit, statuts conserves via `formatLeagueStatusLabel`), `LeagueCard` enfant recoit `t` en prop pour rester un composant pur. Migration `apps/mobile/app/leagues/[id].tsx` : 25+ strings hardcodees remplacees (badge Publique/Privee, meta createur interpolant `{{creator}}` et `{{count}}`, titres sections, scoring labels, season tab `S{{number}} — {{name}}`, journee labels avec/sans nom, headers V/N/D/Pts, summary participant `{{roster}} • {{coach}} • ELO {{elo}}`, erreurs sans/avec prefix). Reutilise `common.retry` pour les boutons Reessayer. Les keys `participants` (sections+scoring) "Participants" et `standings.headers.{wins,draws,losses,points}` (V/N/D/Pts) sont legitimement identiques ou tres proches FR/EN (abreviations universelles de tableau de classement, "Pts" et terme "Participants" inchanges). 102 tests i18n ajoutes (34 FR non-vide + 34 EN non-vide + 27 FR != EN parite stricte + 7 interpolations : `{{message}}` prefix list FR + detail EN, `{{count}}` maxParticipants FR, `{{number}}+{{name}}` tabLabel FR + labelWithName FR, `{{creator}}+{{count}}` creatorMeta EN, `{{roster}}+{{coach}}+{{elo}}` summary FR). Audit baseline tombe a **12 chaines dans 8 fichiers** (-5, -2 fichiers). 1125 tests mobile passent. S27.3.16 (cups screens) — namespace `cups.*` avec 31 cles dans dictionnaires FR/EN reparties en : `public`/`private` (partage liste/archived/detail), `list` (title/archivedLink/filters.all/empty/errors.{loadError,prefix}/card.{participantSingular,participantPlural}), `archived` (title/empty/errors.{loadError,prefix}), `detail` (notFound/creatorMeta, sections.{participants,standings,matches}, participants.{empty,summary}, standings.{empty,headers.{team,wins,draws,losses,points}}, matches.{empty,label}, errors.{loadError,prefix}). Migration `apps/mobile/app/cups/index.tsx` : 8 strings hardcodees remplacees + helper local `buildFilterOptions(t)` (idem leagues) + `CupCard` enfant recoit `t` en prop. Migration `apps/mobile/app/cups/archived.tsx` : 4 strings hardcodees remplacees. Migration `apps/mobile/app/cups/[id].tsx` : 16 strings hardcodees remplacees (badge Publique/Privee, creatorMeta `Cree par {{creator}}`, sections Participants/Classement/Matchs, participants summary `{{roster}} — {{coach}}`, standings headers V/N/D/Pts, label match `Match {{id}}`, mutedText, erreurs). Reutilise `common.retry` pour les boutons Reessayer. Les keys `participantSingular/Plural` ("{{count}} participant(s)") et `matches.label` ("Match {{id}}") sont legitimement identiques FR/EN (mot "participant" identique en francais, "Match" terme universel) ; idem standings.headers V/N/D/Pts (abbreviations partagees). 92 tests i18n ajoutes (31 FR non-vide + 31 EN non-vide + 22 FR != EN parite stricte + 8 interpolations : `{{message}}` prefix list/archived/detail, `{{count}}` participantSingular/Plural FR, `{{creator}}` creatorMeta EN, `{{roster}}+{{coach}}` summary FR, `{{id}}` matches.label FR). Audit baseline tombe a **8 chaines dans 5 fichiers** (-4, -3 fichiers). 1217 tests mobile passent. S27.3.17 (star-players screens) — namespace `starPlayers.*` avec 24 cles dans dictionnaires FR/EN reparties en : `megaStarBadge` (partage liste/detail), `list` (title/subtitleSingular/Plural/Filtered/search.placeholder/ruleset.{season3,season2}/megaToggle.{on,off}/empty/row.hirable/errors.{loadError,prefix}), `detail` (notFound/imageA11y, sections.{stats,skills,hirable,specialRule}, skills.empty, actions.{back,backToCatalog}, errors.loadError). Migration `apps/mobile/app/star-players/index.tsx` : 12 strings hardcodees remplacees + RULESET_OPTIONS desormais reference des `TranslationKey` (resolution via `t(opt.labelKey)` au render), `StarRow` enfant recoit `t` en prop pour rester pur. Migration `apps/mobile/app/star-players/[slug].tsx` : 11 strings hardcodees remplacees (notFound, image a11y interpolant `{{name}}`, sections, skills empty, hirable, regle speciale, retour boutons, badge Mega Star). Reutilise `common.retry` pour les boutons Reessayer. La key `megaToggle.off` ("Mega stars ({{count}})") est legitimement identique FR/EN (terme universel) ; idem `megaStarBadge` ("Mega Star") conserve dans les deux langues. 77 tests i18n ajoutes (24 FR non-vide + 24 EN non-vide + 22 FR != EN parite stricte + 7 interpolations : `{{count}}` subtitleSingular FR + subtitlePlural EN + megaToggle.off FR, `{{total}}` subtitleFiltered FR, `{{hirable}}` row FR, `{{message}}` prefix EN, `{{name}}` imageA11y FR). Audit baseline tombe a **6 chaines dans 3 fichiers** (-2, -2 fichiers). 1294 tests mobile passent. S27.3.18 (match + replay screens) — namespaces `match.*` et `replay.*` avec 43 cles dans dictionnaires FR/EN. `match.history.*` (29 cles) couvre titre/back/empty/scoreFinal interpole `{{teamA}}+{{teamB}}`/stats.{actions,movesPlayed}/errors.{loadError,prefix}/turnRound interpole `{{half}}+{{turn}}`/actionFallback + 8 turnTypes (start/accept/coinToss/selectKickTeam/validateSetup/kickoffSequence/kickoffScatter/kickoffEventResolved) + 11 moveTypes (move/block/blitz/pass/handoff/foul/endTurn/select/chooseBlockResult/choosePushDirection/followUp). `replay.*` (14 cles) couvre loading/empty/back/errors.loadError/halfTurn interpole `{{half}}+{{turn}}`/transport.{start,previous,next,end,play,pause}/speed.{label,a11y interpole `{{label}}`}/actionLabel interpole `{{label}}`. Migration `apps/mobile/app/match/[id].tsx` : helpers locale-agnostic `getTurnTypeLabelKey()` et `getMoveLabelKey()` retournent une `TranslationKey | null` (refactor depuis les anciennes fonctions retournant des strings FR), nouveau helper `resolveTurnLabel(type, moveType, t)` qui appelle `t()` avec fallback `match.history.actionFallback`. `TurnCard` recoit `t` en prop pour rester pur. 8 strings hardcodees remplacees dans la UI (titre, back, score final, stats, vide, erreurs, MT/Tour). Migration `apps/mobile/app/replay/[id].tsx` : 13 strings hardcodees remplacees (loading, errors fallback, retour boutons, mi-temps/tour, transport a11y x6, vitesse label + a11y interpolee, action label). Reutilise `common.error` pour le titre d'erreur et `common.retry` pour le bouton de retry. Les keys "Blitz", "Selection" et "Pause" sont legitimement identiques FR/EN (termes universels). 132 tests i18n ajoutes (43 FR non-vide + 43 EN non-vide + 40 FR != EN parite stricte + 6 interpolations : `{{teamA}}+{{teamB}}` scoreFinal FR, `{{half}}+{{turn}}` turnRound EN + halfTurn FR, `{{message}}` prefix FR, `{{label}}` speed.a11y EN + actionLabel FR). Audit baseline tombe a **3 chaines dans 1 fichier** (-3, -2 fichiers). 1426 tests mobile passent. S27.3.19 (cloture S27.3 : `_layout.tsx`) — namespace `nav.*` avec 19 cles dans dictionnaires FR/EN couvrant les 19 titres de Stack.Screen (appName/lobby/matchmaking/leaderboard/login/register/matchHistory/replay/teamsList/teamsNew/teamsDetail/cupsList/cupsArchived/cupsDetail/leaguesList/leaguesDetail/starPlayersList/starPlayersDetail/settings). Refactor `_layout.tsx` : extraction d'un sous-composant `AppStack` interne au `LocaleProvider` qui consomme `useTranslation()` et resout chaque titre via `t("nav.*")`. Le composant `Layout` reste l'enveloppe externe (GestureHandlerRootView -> LocaleProvider -> AuthProvider -> PushRegistration -> AppStack), garantissant que `t` est dispo au moment du rendu des Screens. Les keys "Nuffle Arena" (nom propre), "Star Players"/"Star Player" (termes Blood Bowl) et "Replay" (terme universel) sont legitimement identiques FR/EN. 53 tests i18n ajoutes (19 FR non-vide + 19 EN non-vide + 15 FR != EN parite stricte). **Audit baseline tombe a 0 chaine hardcodee dans 0 fichier (-3, -1 fichier) — objectif "Mobile : 100% des strings en i18n" atteint.** 1479 tests mobile passent.** |
| S27.4 | Mobile : accessibilite touch board + hooks composites | AMELIO | M | [x] | `apps/mobile/app/play/[id].tsx`. Ajouter `accessibilityRole`, `accessibilityLabel` sur PixiBoardNative. Consolider `use-game-socket` + `use-game-chat` -> `useGameMatch`. **TERMINE : S27.4.1 (a11y board) — helper pur `summarizeBoardForA11y(state)` (`apps/mobile/lib/board-a11y.ts`) qui extrait `{scoreA, scoreB, half, turn, currentTeam, teamA, teamB, playersA, playersB}` depuis `GameState` (compte les joueurs `on-field` ou sans `state` defini). Ajout namespace `play.a11y.*` (boardLabel interpolee + boardHint) dans dictionnaires FR/EN. PixiBoardNative accepte deux nouveaux props optionnels `accessibilityLabel` et `accessibilityHint` ; le `View` racine utilise `accessibilityRole="adjustable"` (zoom/pan + tap), `accessible`, et expose label/hint au screen reader. `play/[id].tsx` injecte les valeurs localisees via `t("play.a11y.boardLabel", summarizeBoardForA11y(state))` + `t("play.a11y.boardHint")`. 5 tests `board-a11y.test.ts` + 4 nouveaux tests `i18n-keys-play.test.ts` (boardLabel + boardHint en FR + EN, interpolation FR + EN). S27.4.2 (hooks composites) — nouveau hook `useGameMatch({matchId, ...socketOptions})` (`apps/mobile/lib/use-game-match.ts`, ~50l) qui compose `useGameSocket` + `useGameChat` derriere une seule API plate (`connected`, `joined`, `error`, `reconnecting`, `reconnectAttempt`, `submitMove`, `chatMessages`, `sendChatMessage`). La socket sous-jacente n'est plus exposee : tout passe par les helpers retournes. `play/[id].tsx` migre vers `useGameMatch` : suppression de l'import `useGameChat` separe, fusion des deux blocs en un seul appel. 1492 tests mobile passent.** |
| S27.5 | Skeleton Season 4 BB3 (pattern S3 = clone + overrides) | EVO | M | [x] | `packages/game-engine/src/rosters/`. Skeleton infra pret a recevoir les rosters officiels GW 2025-2026 sans toucher a S2/S3. **TERMINE : `Ruleset` etendu a `"season_2" \| "season_3" \| "season_4"`, `RULESETS` inclut `season_4`, `DEFAULT_RULESET` reste `season_3`. Nouveau module `season4-rosters.ts` (+ pendant JS) exposant `SEASON_FOUR_ROSTERS = {}` (vide) avec doc d'usage. Dans `star-players.ts`, ajout `SEASON_FOUR_STAR_PLAYER_OVERRIDES = {}` et `buildSeasonFourStarPlayers()` qui clone profondement S3 et applique les overrides (aucune reference partagee avec S2/S3). `STAR_PLAYERS_BY_RULESET.season_4` et `TEAM_REGIONAL_RULES_BY_RULESET.season_4` cables. Cote serveur, Zod `createLeagueSchema.ruleset` et le type `CreateLeagueInput.ruleset` etendus a `season_4`. 7 nouveaux tests `season4-rosters.test.ts` (RULESETS contient season_4, type accepte, SEASON_FOUR_ROSTERS vide, clone de S3 sans reference partagee, mutation S4 ne propage pas a S3/S2). 4849 tests game-engine + 1726 tests server passent. Schema Prisma `Season4*` non requis : la DB stocke `ruleset` en string libre, deja flexible.** |
| S27.6 | Audit log admin (table AuditLog : qui change quoi sur skills/rosters/users) | EVO | L | [x] | Aucune trace actuelle des actions admin (create/update/delete contenu). Compliance + tracabilite. Modele Prisma `AuditLog(userId, action, entity, entityId, oldValue Json?, newValue Json?, createdAt)`. Hook dans toutes les routes `/admin/data/*`. UI lecture `/admin/audit-log`. **TERMINE : S27.6.1 (foundation) — modele Prisma `AuditLog(id, userId?, action, entity, entityId?, oldValue Json?, newValue Json?, ipAddress?, userAgent?, createdAt)` ajoute aux schemas Postgres (`prisma/schema.prisma`) et SQLite (`apps/server/prisma/sqlite/schema.prisma` ; `oldValue`/`newValue` en `String` car SQLite n'a pas de JSONB natif). Migration `20260507400000_add_audit_log` SQL Postgres avec 4 index (`userId, createdAt`, `entity, entityId`, `action`, `createdAt`). Service `apps/server/src/services/audit-log.ts` exposant `recordAdminAction(prisma, input)` : serialise `oldValue`/`newValue` en JSON string (ou `Prisma.JsonNull` quand absent) pour eviter le branching par provider. 8 tests (champs requis, defauts, serialisation objet/tableau/primitif, undefined -> JsonNull cote creation/suppression, ipAddress + userAgent, propagation d'erreur Prisma). 1734 tests server passent. S27.6.2 (cabler les routes admin) — helpers `extractRequestContext(req)` (IP + User-Agent tronque a 512 chars, fallback null sur valeurs vides ou non-string) et `recordAdminActionFromRequest(prisma, req, partial)` qui remplit `userId`/`ipAddress`/`userAgent` automatiquement depuis la requete authentifiee. Wrapper `safeAudit(req, partial)` interne a `admin.ts` qui swallow les erreurs d'audit log et serverLog.error : une defaillance de l'audit ne doit pas faire echouer une mutation deja committee. Cablage de 8 routes critiques de `apps/server/src/routes/admin.ts` : `PATCH /users/:id/role` (snapshot avant `{role, roles}` + apres `{role, roles}`), `PATCH /users/:id/patreon` (avant/apres `{patreon}`), `PATCH /users/:id/valid` (avant/apres `{valid}`), `DELETE /users/:id` (snapshot complet old, pas de new), `PATCH /matches/:id/status` (avant/apres `{status}`), `DELETE /matches/:id` (snapshot id/status/seed/createdAt), `POST /matches/purge` (snapshot count avant), `DELETE /teams/:id` (snapshot id/name/ownerId). 6 nouveaux tests (extractRequestContext x4 + recordAdminActionFromRequest x2). 14 tests audit-log totaux, 1740 tests server passent. S27.6.3 (UI lecture) — helper pur `parseAuditLogQuery(query)` qui parse `limit/page/userId/action/entity` avec clamp `[1, 200]` sur limit, fallback sur valeurs invalides, filtres ignores si vides. Route `GET /admin/audit-log` (admin-only) cablee : `count` + `findMany` paginated, tri `createdAt desc`. Cote web : client API `apps/web/app/lib/auditLog.ts` (`fetchAuditLog(query)`), page `apps/web/app/admin/audit-log/page.tsx` avec table FR (Date/Admin/Action/Entite/Cible/IP/Details), formulaire de filtres (action/entity/userId), pagination Precedent/Suivant, expand par ligne pour voir oldValue/newValue prettifiees + UA. Liens nav layout (`📜 Journal d'audit`). Ruleset `season_4` propage cote web (RULESET_LABELS, teams/new rulesetLabels, team-structured-data). 8 nouveaux tests (`parseAuditLogQuery` : defaults, parse valides, clamp limit, fallback page, filtres complets/partiels, ignore vides, calcul skip). 22 tests audit-log totaux, 1748 tests server passent. S27.6.4 (cabler `/admin/data/*`) — extraction du wrapper `safeRecordAdminActionFromRequest(prisma, req, partial, options?)` dans `services/audit-log.ts` (logger injectable, swallow l'erreur d'audit pour ne pas faire echouer la mutation deja committee). `admin.ts` migre son `safeAudit` local vers le helper partage. Cablage de 15 routes mutation dans `routes/admin-data.ts` : Skills (`POST/PUT/POST :id/duplicate/DELETE`), Rosters (idem x4), Positions (idem x4), Star Players (`POST/PUT/DELETE`). Chaque route capture `previous` avant mutation (UPDATE/DELETE) et logge `oldValue` + `newValue` snapshots avec actions normalisees (`skill.create`, `roster.update`, `position.duplicate`, `star-player.delete`, etc.). 4 nouveaux tests `safeRecordAdminActionFromRequest` (propagation succes / swallow erreur / logger inject / pas de log si succes). 1759 tests server passent. DoD `Hook dans toutes les routes /admin/data/*` desormais satisfaite.** |
| S27.7 | B0.1 residuels : Stunty AV, GFI Sure Feet/Sprint, Horns vers registry | CONFORT | M | [x] | Solde [`docs/roadmap/follow-up-b01.md`](../follow-up-b01.md). Etendre `SkillContext.currentTrigger` pour discriminer Stunty (on-dodge ET on-armor). Refactor `blocking.ts:141`, `movement.ts` (aucun import skill-bridge), `foul.ts` (Sneaky Git mixte). **EN COURS : S27.7.1 (Stunty AV via registry) — extension `SkillContext` avec `currentTrigger?: SkillTrigger` rempli automatiquement par `collectModifiers(player, trigger, ctx)`. L'entree Stunty etend `triggers: ['on-dodge', 'on-armor']` et `getModifiers` discrimine via `ctx.currentTrigger` (-1 armor mod sur on-armor, +1 dodge mod sur on-dodge). `mechanics/blocking.ts:141` remplace `hasSkill(victim, 'stunty') ? -1 : 0` par `collectModifiers(victim, 'on-armor', { state, opponent: attacker }).armorModifier ?? 0`. 8 tests `stunty-armor.test.ts` (triggers exposes, on-armor sur Stunty/non-Stunty, on-dodge isole, isolation des modifiers entre triggers, currentTrigger rempli). 4857 tests game-engine + 1748 tests server passent. S27.7.2 (Sprint GFI cap via registry) — Sure Feet etait deja cable via `canSkillReroll(player, 'on-gfi', state)` dans `actions.ts:1192`. Sprint en revanche etait registre avec `getModifiers: () => ({ movementModifier: 1 })` mais jamais consomme — tracedu bug initial dans le follow-up doc. Ajout du modifier `gfiCapBonus?: number` dans `SkillModifier`. Sprint expose desormais `gfiCapBonus: 1` sur `on-gfi`. Nouveau helper pur `getGfiCap(state, player): number` (`core/game-state.ts`) qui retourne `2 + (collectModifiers(player, 'on-gfi', { state }).gfiCapBonus ?? 0)`. Trois call-sites consommateurs migres : `canPlayerMove` (game-state.ts:935), `canPlayerContinueMoving` (game-state.ts:959), `getReachableCells` (tactical-indicators.ts:36) ; tous passaient par `2 - gfiUsed` ou `gfiUsed < 2` hardcoded. 8 tests `sprint-gfi-cap.test.ts` (Sprint expose gfiCapBonus + plus movementModifier, collectModifiers Sprint/non-Sprint, getGfiCap defaut 2 / Sprint 3 / autres skills neutres / Sure Feet ne touche pas le cap). 4865 tests game-engine + 1755 tests server passent. S27.7.3 (Horns/Pile Driver/Sneaky Git, cloture S27.7) — extension `SkillContext.isBlitz?: boolean`. Horns canApply requiert desormais `ctx.isBlitz === true` (le `+1 ST` ne s'applique que sur un Blitz, pas sur un Block standard). Cablage dans `actions.ts` autour du calcul de `attackerStrength` : `collectModifiers(attacker, 'on-block-attacker', { state, opponent: target, isBlitz: isBlitzDuringMove }).strengthModifier`. Pile Driver passe en trigger `passive` (etait `on-block-attacker` mais sans modifier — bruit dans `getSkillsForTrigger`). Sneaky Git unifie : `core/game-state.ts:982` remplace `hasSkill(player, 'sneaky-git') || hasSkill(player, 'sneaky_git')` par `isSneakyGitActive(state, player)` (chemin registry deja utilise dans `foul.ts`). Multiple Block migre simultanement vers le registry : `canApply` se gate sur `state.pendingMultipleBlock?.attackerId === ctx.player.id` (anti double-comptage avec l'ancienne `multipleBlockPenalty`). `actions.ts` consomme desormais `multipleBlockPenalty` ET Horns via le meme `collectModifiers`. 10 nouveaux tests `horns-pile-driver-sneaky.test.ts` (Horns gate sur isBlitz, Pile Driver passe a passive et n'apparait plus dans on-block-attacker, Sneaky Git registry). Test `batch-3k-registry.test.ts` mis a jour pour fournir `pendingMultipleBlock` au cas multiple-block. 4875 tests game-engine + 1755 tests server passent.** |
| S27.8 | Refactor `actions.ts` (2695l) + `match.ts` (1605l) + `team.ts` (2150l) en micro-modules | CONFORT | XL | [~] | Etalable post-S27 si trop dense. `actions/{move,block,pass,foul,blitz,handoff}.ts` + `index.ts` unificateur. `match.ts` -> `match/{game,state,actions,prematch}.ts`. Pre-requis avant de poser de nouvelles features sur ces fichiers. **EN COURS : S27.8.24 (troisieme slice sur `team.ts` : extraction des 4 handlers Player CRUD) — `handleAddTeamPlayer` (`POST /:id/players` avec validation budget/slots/numero unique/position max), `handleDeleteTeamPlayer` (`DELETE /:id/players/:playerId` avec exception funeral L2.B.6), `handleUpdatePlayerSkills` (`PUT /:id/players/:playerId/skills` avec advancement primary/secondary chosi/random + 6-cap + SPP cost + category access), `handleListAvailablePositions` (`GET /:id/available-positions` avec compteurs + canAdd flag) extraits dans `routes/team-player-handlers.ts` (578l, au-dessus du soft DoD 400 mais cohesion forte 4 handlers Player CRUD complexes). Re-export depuis `team.ts`. Aucun cycle : helpers leaf (`prisma`, `sendError`/`sendSuccess`, `updateTeamValues`, `AllowedRoster`/`Ruleset`/`DEFAULT_RULESET`/`AdvancementType`/`PlayerAdvancement`/`getNextAdvancementPspCost`/`getPositionCategoryAccess`/`SKILLS_BY_SLUG`/`SKILLS_DEFINITIONS` from `@bb/game-engine`, `getRosterFromDb`, `serverLog`, dynamic import `getPlayerCost`). Cleanup d'imports orphelins dans `team.ts` (6 retires) : `getNextAdvancementPspCost`, `getPositionCategoryAccess`, `SKILLS_BY_SLUG`, `SKILLS_DEFINITIONS`, `AdvancementType`, `PlayerAdvancement`. `team.ts` 1945 -> 1485 lignes (-460 lignes nettes ; cumul -929 vs origine 2414 sur team.ts depuis S27.8.22, -38%). 7 nouveaux smoke tests `team-player-handlers.test.ts` (exports x2 dont re-export depuis `team.ts`, gates 404 `Equipe introuvable` x3, gates 400 body manquant x2). 1814 tests server passent (was 1807, +7 nouveaux smoke). Typecheck clean. S27.8.23 (deuxieme slice sur `team.ts` : extraction des 4 handlers Star Players) — `handleListTeamStarPlayers` (`GET /:id/star-players`), `handleListAvailableStarPlayers` (`GET /:id/available-star-players` avec budget+slots+paires), `handleHireStarPlayer` (`POST /:id/star-players` avec gestion paires + lock match en cours), `handleDeleteTeamStarPlayer` (`DELETE /:id/star-players/:starPlayerId` avec retrait de la paire eventuelle) extraits dans `routes/team-star-player-handlers.ts` (488l, legerement au-dessus du soft DoD 400 mais cohesion forte 4 handlers CRUD Star Players). Re-export depuis `team.ts`. Aucun cycle : helpers leaf (`prisma`, `sendError`/`sendSuccess`, `updateTeamValues`, `getStarPlayerBySlug`/`DEFAULT_RULESET`/`Ruleset` from `@bb/game-engine`, `validateStarPlayerHire`/`getTeamAvailableStarPlayers`/`requiresPair` from `../utils/star-player-validation`, `serverLog`, dynamic import `getPlayerCost` from `team-value-calculator`). Cleanup d'imports orphelins dans `team.ts` (3 retires) : `validateStarPlayerHire`, `getTeamAvailableStarPlayers`, `requiresPair`. `team.ts` 2335 -> 1945 lignes (-390 lignes nettes ; cumul -469 vs origine 2414 sur team.ts depuis S27.8.22, -19%). 6 nouveaux smoke tests `team-star-player-handlers.test.ts` (exports x2 dont re-export depuis `team.ts`, gates 404 `Equipe introuvable` x4). 1807 tests server passent (was 1801, +6 nouveaux smoke). Typecheck clean. S27.8.22 (premier slice sur `apps/server/src/routes/team.ts` 2414l : extraction des 4 handlers de lecture seule) — `handleGenerateTeamName` (`GET /name-generator` utilitaire pur), `handleListAvailableTeams` (`GET /available` filtrable par ruleset), `handleListMyTeams` (`GET /mine` pagine), `handleGetRoster` (`GET /rosters/:id` lookup roster) extraits dans `routes/team-readonly-handlers.ts` (195l, sous DoD secondaire `<= 400`). Re-export depuis `team.ts` pour preserver l'API publique consommee par `team.test.ts`. Aucun cycle : helpers leaf (`prisma`, `sendError`/`sendSuccess`, `Ruleset`, `resolveRuleset`/`isValidRuleset`, `getRosterFromDb`, `parsePagination`/`buildApiMeta`, `generateTeamName`). Cleanup d'imports orphelins dans `team.ts` (4 retires) : `isValidRuleset`, `parsePagination`, `buildApiMeta`, `generateTeamName`. `team.ts` 2414 -> 2335 lignes (-79 lignes nettes ; premier de plusieurs slices a venir pour ramener `team.ts` sous une cible raisonnable). 6 nouveaux smoke tests `team-readonly-handlers.test.ts` (exports x2 dont re-export depuis `team.ts`, defaut roster `generateTeamName`, passthrough roster query, gates 404 `Roster inconnu` x2). 1801 tests server passent (was 1795, +6 nouveaux smoke). Typecheck clean. S27.8.21 (sixieme slice sur `match.ts` : extraction des 3 handlers de la sequence de kickoff) — `handlePlaceKickoffBall` (`POST /:id/place-kickoff-ball`), `handleCalculateKickDeviation` (`POST /:id/calculate-kick-deviation`), `handleResolveKickoffEvent` (`POST /:id/resolve-kickoff-event`) extraits dans `routes/match-kickoff-handlers.ts` (346l). 3 handlers thematiquement coheressents (sequence de kickoff : place ball -> calcul deviation -> resolution evenement -> demarrage match avec `scheduleAILoop` si IA receveuse). Re-export depuis `match.ts` pour preserver l'API publique consommee par les tests d'integration. Aucun cycle : helpers leaf (`prisma`, `scheduleAILoop`, `serverLog`, dynamic imports `@bb/game-engine` pour `placeKickoffBall`/`calculateKickDeviation`/`resolveKickoffEvent`/`startMatchFromKickoff`/`makeRNG`, `services/game-broadcast.broadcastGameState`). `match.ts` 705 -> 491 lignes (-214 lignes nettes ; cumul -1275 vs origine 1766 sur match.ts depuis S27.8.16, -72%). 8 nouveaux smoke tests `match-kickoff-handlers.test.ts` (exports x2 dont re-export depuis `match.ts`, gates 404 `match introuvable` x3, gates 400 `phase incorrecte` x3). 1795 tests server passent. Typecheck clean. Reste dans `match.ts` : `handlePracticeMatch`, `handleSubmitMove` + `validate-setup` inline. S27.8.20 (cinquieme slice sur `match.ts` : extraction des 4 handlers de cycle de vie) — `handleCreateMatch` (`POST /create`), `handleJoinMatch` (`POST /join`), `handleAcceptMatch` (`POST /accept`), `handleCancelMatch` (`POST /:id/cancel`) extraits dans `routes/match-lifecycle-handlers.ts` (179l). Re-export depuis `match.ts`. Aucun cycle : helpers leaf (`prisma`, `jwt`, `MATCH_SECRET`, `acceptAndMaybeStartMatch`, `cancelMatch`, `serverLog`, `sendError`/`sendSuccess`). `match.ts` 809 -> 705 lignes (-104 lignes nettes ; cumul -1061 vs origine 1766 sur match.ts depuis S27.8.16, -60%). 1787 tests server passent. Typecheck clean. Reste dans `match.ts` : `handlePracticeMatch`, `handleSubmitMove` + 4 routes inline setup/kickoff. S27.8.19 (quatrieme slice sur `match.ts` : extraction des 5 handlers details/list) — `handleGetMatchDetailsByToken` (`GET /details` token-based), `handleGetMatchDetails` (`GET /:id/details`), `handleGetMatchTeams` (`GET /:id/teams`), `handleListMyMatches` (`GET /my-matches`), `handleGetMatchSummary` (`GET /:id/summary`) extraits dans `routes/match-details-handlers.ts` (464l). 5 handlers de lecture seule cohesifs : noms d'equipes/coachs, ELO, listing perso, summary prematch. Re-export depuis `match.ts` pour preserver l'API. Aucun cycle : helpers leaf (`prisma`, `jwt`, `MATCH_SECRET`, `serverLog`, `sendError`/`sendSuccess`). `match.ts` 1176 -> 809 lignes (-367 lignes nettes ; cumul -957 vs origine 1766 sur match.ts depuis S27.8.16, -54%). 1787 tests server passent. Typecheck clean. S27.8.18 (troisieme slice sur `match.ts` : extraction `handleGetMatchState`) — handler `GET /:id/state` (~221l) extrait dans `routes/match-state-handler.ts` (285l). Le handler couvre la transition idle->setup atomique (`ensureSetupPhasePersisted`), declenchement IA setup (`runAISetupIfNeeded`), selection du gameState canonique selon `match.status` (prematch/prematch-setup/active), reconstruction prematch via `setupPreMatchWithTeams`, et enrichissement de la reponse (currentTeam, isMyTurn, myTeamSide, moveCount, lastMoveAt). Re-export depuis `match.ts` pour preserver l'API publique consommee par les tests d'integration. Aucun cycle vers `match.ts` : helpers leaf (`prisma`, `ensureSetupPhasePersisted`, `runAISetupIfNeeded`, `getUserTeamSide`, `serverLog`, `sendError`/`sendSuccess`, `setupPreMatchWithTeams` via dynamic import). `match.ts` 1393 -> 1176 lignes (-217 lignes nettes ; cumul -590 vs origine 1766 sur match.ts depuis S27.8.16). 1787 tests server passent. Typecheck clean. S27.8.17 (deuxieme slice sur `match.ts` : extraction des 3 handlers de lecture restants `handleListMatchTurns` / `handleGetMatchResults` / `handleListLiveMatches`) — module `routes/match-readonly-handlers.ts` (302l) regroupant les 3 endpoints `GET /:id/turns`, `GET /:id/results`, `GET /live`. Re-export depuis `match.ts` pour preserver l'API publique consommee par les tests d'integration. Aucun cycle vers `match.ts` : dependances leaf (`prisma`, `getSpectatorCount`, `serverLog`, `sendError`/`sendSuccess`, `AuthenticatedRequest`). `match.ts` 1631 -> 1393 lignes (-238 lignes nettes ; cumul -373 vs origine 1766 sur match.ts depuis S27.8.16). 1787 tests server passent. Typecheck clean. Reste a extraire le gros morceau `handleGetMatchState` (~685l). S27.8.16 (premier slice sur `apps/server/src/routes/match.ts`) — extraction des handlers de lecture en lecture seule `handleSpectateMatch` (`GET /:id/spectate`) et `handleReplayMatch` (`GET /:id/replay`) dans le nouveau module `routes/match-spectate-replay-handlers.ts` (189l). Re-export depuis `match.ts` pour preserver l'API publique consommee par les tests d'integration. Aucun cycle vers `match.ts` : les 2 handlers ne dependent que de `prisma`, `getSpectatorCount`, `serverLog`, `sendError/sendSuccess`, `AuthenticatedRequest`. `match.ts` 1766 -> 1631 lignes (-135 lignes nettes). 1787 tests server passent. Typecheck clean. Premier de plusieurs slices a venir pour ramener `match.ts` sous une cible raisonnable. S27.8.15 (split `block-action.ts` : extraction `handleBlock`) — `handleBlock` (le plus gros handler de `block-action.ts`, ~200l) extrait dans le nouveau module `actions/block-handler.ts` (252l). Re-export depuis `block-action.ts` pour preserver l'API consommee par `actions.ts` et les tests. Aucun cycle vers `block-action.ts` : `handleBlock` est appele par `handleDumpOffChoose`, `handleMultiBlock`, `resolveFrenzyBlock`, `resolveMultipleBlock` (tous restes dans `block-action.ts`) — dependance unidirectionnelle. Cleanup d'imports orphelins dans `block-action.ts` (10+ retires) : `setPlayerAction`, `canTeamBlitz`, `canBlock`, `calculateOffensiveAssists`, `calculateDefensiveAssists`, `calculateBlockDiceCount`, `getBlockDiceChooser`, `resolveBlockResult`, `canDumpOff`, `getDumpOffReceivers`, `checkFoulAppearance`, `checkDauntless`, `collectModifiers`, `blockResultFromRoll`, `rollBlockDiceManyWithNotification` (toutes les dependances metier de `handleBlock` qui ne sont plus utilisees par les autres fonctions du module). `block-action.ts` 503 -> 283 lignes (**-220 lignes nettes**, **sous le seuil DoD secondaire `<= 400 lignes`**). **TOUS les modules `actions/` sont desormais <= 400 lignes** (sauf `move-handlers.ts` a 389l qui est sous le seuil). 5 nouveaux smoke tests `block-handler.test.ts` (export du nouveau module, re-export valide depuis `block-action.ts`, gates `attaquant introuvable` / `cible introuvable`, immutabilite). Bonus : suppression de 13 fichiers `.js` stales dans `src/actions/` qui shadow les `.ts` au runtime vitest (meme quirk corrige dans la PR #683 sur `evaluator.js`). Coverage exclusion ajoutee dans `vitest.config.ts` (meme quirk source-map v8). 4933 tests game-engine + 1787 tests server passent. Typecheck + lint + build clean. `match.ts` (1605l) et `team.ts` (2150l) non touches dans ce slice (DoD reste `[~]` jusqu'a leur traitement). S27.8.14 (split `choice-handlers.ts` : extraction `handleRerollChoose`) — `handleRerollChoose` (le plus gros handler de `choice-handlers.ts`, ~199l incluant les helpers prives `getLonerThreshold` et `consumeTeamReroll`) extrait dans le nouveau module `actions/reroll-choose-handler.ts` (270l). Re-export depuis `choice-handlers.ts` pour preserver l'API consommee par `actions.ts` et les tests. Aucun cycle vers `actions.ts` ni vers `choice-handlers.ts` : le handler appelle uniquement `mechanics/*`, `core/*`, `utils/*`, `skills/*`, `actions/failure-helpers`, `actions/ball-pickup`. Cleanup d'imports orphelins dans `choice-handlers.ts` : `Player`, `isInOpponentEndzone`, `awardTouchdown`, `samePos` (4 retires). Cleanup additionnel : `_targetNumber` (artifact legacy de destructuration jamais utilisee) retire de `pendingReroll` dans le nouveau module. `choice-handlers.ts` 517 -> 288 lignes (**-229 lignes nettes**, sous le seuil DoD secondaire `<= 400 lignes`). 5 nouveaux smoke tests `reroll-choose-handler.test.ts` (export depuis le nouveau module, re-export depuis `choice-handlers.ts` valide, gates `pendingReroll absent` x2, immutabilite). Coverage exclusion ajoutee dans `vitest.config.ts` (meme quirk source-map v8). 4928 tests game-engine + 1787 tests server passent. Typecheck + lint + build clean. S27.8.13 (extraction `handleBlitz`) — le plus gros handler restant (~226l) extrait dans `actions/blitz-handler.ts` (282l). Flux complet preserve : Foul Appearance check defensif, mouvement avec dodge eventuel (Shadowing + Break Tackle BB3 + jet d'armure + injury sur echec + bounce ball), blocage post-mouvement (assists, dice count, chooser, pendingBlock). Aucun cycle vers `actions.ts` (helpers leaf uniquement : `mechanics/blocking`, `mechanics/movement`, `mechanics/ball`, `mechanics/shadowing`, `mechanics/break-tackle`, `mechanics/injury`, `mechanics/negative-traits.checkFoulAppearance`, `core/game-state`, `utils/dice`, `utils/dice-notifications`, `utils/logging`, `skills/skill-bridge`). Cleanup massif d'imports orphelins dans `actions.ts` (25 retires) : `Position`, `getDodgeSkillModifiers`, `requiresDodgeRoll`, `calculateDodgeModifiers`, `rollBlockDiceManyWithRolls`, `performDodgeRollWithNotification`, `performArmorRollWithNotification`, `performInjuryRoll`, `createLogEntry`, `isInOpponentEndzone`, `awardTouchdown`, `bounceBall`, `canBlock`, `canBlitz`, `calculateOffensiveAssists`, `calculateDefensiveAssists`, `calculateBlockDiceCount`, `getBlockDiceChooser`, `setPlayerAction`, `checkPlayerTurnEnd`, `handlePlayerSwitch`, `incrementTeamBlitzCount`, `checkBreakTackle`, `resolveShadowingAfterDodge`, `checkFoulAppearance`. `actions.ts` 626 -> 324 lignes (**-302 lignes nettes** ; cumul -2382 vs origine 2706 = **~88 % reduction**). **DoD `actions.ts <= 600 lignes` ATTEINT.** 5 nouveaux smoke tests `blitz-handler.test.ts` (export, gates `attaquant introuvable` / `cible introuvable` / `cible amie illegale`, immutabilite). Coverage exclusion ajoutee dans `vitest.config.ts` (meme quirk source-map v8 que les autres extractions S27.8.x). 4923 tests game-engine + 1787 tests server passent. Typecheck + lint + build clean (0 warnings). `match.ts` et `team.ts` non touches dans ce slice (etalable). S27.8.12 (extraction `handleLeap` + `handleMove` + `handleDodge` + `handleDumpOffChoose`) — apres S27.8.11 qui a sorti `getLegalMoves` dans son propre module, le cycle d'import qui empechait jusque-la l'extraction des 3 handlers `handleLeap`/`handleMove`/`handleDodge` (qui appellent `getLegalMoves` pour valider la legalite du move) est leve. Nouveau module `actions/move-leap-dodge-handlers.ts` (292l) qui regroupe ces 3 handlers thematiquement coherents (handleLeap = saut + AG roll + ball pickup ; handleMove = simple move + dispatch dodge/normal ; handleDodge = jet d'esquive explicite + shadowing + break-tackle). `handleDumpOffChoose` (53l) rejoint `actions/block-action.ts` (cohesion : il termine en relancant `handleBlock` avec `skipDumpOff: true`). Aucun cycle vers `actions.ts` : tous les helpers proviennent de `mechanics/*`, `core/*`, `actions/legal-moves`, `actions/move-handlers`, `actions/ball-pickup`, `actions/failure-helpers`. Cleanup d'imports orphelins dans `actions.ts` : `hasSkill`, `samePos`, `applyRollFailure`, `handleBallPickup`, `handleDodgeRoll`, `handleNormalMove`, `executeDumpOff`, `getLeapModifier`, `performLeapRoll` (9 imports retires en suivant les extractions). `actions.ts` 896 -> 626 lignes (-270 lignes nettes ; cumul -2080 vs origine 2706 = **~77 % reduction**, a 26 lignes de la cible DoD `<= 600`). `block-action.ts` 448 -> 503 lignes (+53 nettes pour le nouveau handler ; sous le seuil 800 du DoD). 8 nouveaux smoke tests `move-leap-dodge-handlers.test.ts` (3 handlers exportes, gates `playerId inexistant` x3, immutabilite x2). 4918 tests game-engine + 1787 tests server passent. Typecheck + lint + build clean (0 warnings sur les fichiers modifies). S27.8.11 (extraction `getLegalMoves` + helper `getAdjacentOpponents`) — module dedie `actions/legal-moves.ts` (326l) avec re-export depuis `actions.ts` pour preserver l'API publique consommee par `index.ts`, `ai/difficulty.ts`, `ai/evaluator.ts`, `utils/referee.ts`. Variante locale de `getAdjacentOpponents` privee au nouveau module (ne filtre que `stunned`, contrairement a la version `mechanics/movement.ts` qui exclut hypnotises et `titchy`) : comportement preserve a l'identique. Aucun cycle vers `actions.ts` (tous les helpers viennent de `mechanics/*` ou `core/*`). Cleanup massif d'imports orphelins : 5 lies a mon extraction (`getPassRange`, `canAttemptPassForRange`, `canThrowTeamMate`, `getThrowRange`, `isAdjacent`, `canHypnoticGaze`, `canProjectileVomit`, `canStab`, `canChainsaw`, `canLeap as playerCanLeap`, `getLegalLeapDestinations`, `canInstablePerformAction`, `logInstablePrevention`, `Player`, `TeamId`, `inBounds`, `canPlayerMove`, `canPlayerContinueMoving`, `shouldAutoEndTurn`, `canTeamBlitz`) + 5 preexistants reperes en bonus (`applyPickupFailure`, `canDumpOff`, `getDumpOffReceivers`, `checkDauntless`, `getArmBarBonus`). `actions.ts` 1165 -> 896 lignes (-269 lignes nettes ; cumul -1810 vs origine 2706 = **~67 % reduction**). 9 nouveaux smoke tests `legal-moves.test.ts` (API exportee, gate `gamePhase===ended`, gate `players` manquant, gate `isTurnover`, gate `pendingApothecary`, gate `pendingReroll`, gate `pendingKickoffEvent`, fallback `END_TURN`, immutabilite). 4910 tests game-engine + 1787 tests server passent. Typecheck + lint + build clean. S27.8.10 (multi-block + frenzy second bloc) — extraction de `handleMultiBlock` (~37l), `resolveMultipleBlock` (~79l) et `resolveFrenzyBlock` (~34l) depuis `actions.ts` vers `actions/block-action.ts`. Les trois fonctions sont block-related et dependent de `handleBlock` (deja dans `block-action.ts`) et de helpers leaf (`canPerformMultipleBlock`/`markMultipleBlockUsed` from `mechanics/multiple-block`, `isAdjacent`, `createLogEntry`) — aucune dependance vers `actions.ts`, donc pas de cycle. Imports orphelins retires de `actions.ts` : `canPerformMultipleBlock`, `markMultipleBlockUsed`. Le dispatcher `applyMove` re-importe les 3 fonctions depuis `block-action.ts`. `actions.ts` 1329 -> 1165 lignes (-164 lignes nettes ; cumul -1541 vs origine 2706 = ~57% reduction). 13 nouveaux smoke tests `block-action.test.ts` (exports presents, gates `resolveFrenzyBlock` x5 : no pending / push choice en attente / follow-up choice en attente / acteur ou cible introuvable / non-adjacent ; gates `resolveMultipleBlock` x5 : no pending / pendingBlock en cours / secondTargetId absent / turnover / non-adjacent avec log d'annulation ; gates `handleMultiBlock` x2 : attaquant introuvable / pas de skill multiple-block). 4901 tests game-engine + 1787 tests server passent. S27.8.1 (handlers d'actions speciales) — extraction de 6 handlers homogenes (handleHypnoticGaze, handleProjectileVomit, handleStab, handleChainsaw, handleBallAndChain, handleBombThrow) depuis `packages/game-engine/src/actions/actions.ts` vers le nouveau module `actions/special-actions.ts` (158l, exports nommes). Chaque handler suit la meme structure (gates team/hasPlayerActed/can*, execute via mecanique dediee, setPlayerAction + checkPlayerTurnEnd). Aucun couplage cross-handler : extraction triviale. `actions.ts` 2706 -> 2595 lignes (-111 lignes nettes, ~4% reduction). Coverage : `actions.ts` + extractions ajoutees a la liste d'exclusion `vitest.config.ts` (quirk source-map v8, deja documente pour `skill-registry.ts` etc.) ; 13 smoke tests `special-actions.test.ts` ajoutes. 4888 tests game-engine + 1755 tests server passent. S27.8.2 (famille pass) — extraction de 5 handlers cohesifs (handlePass + handleOnTheBallMove + handleOnTheBallDecline + handleHandoff + handleThrowTeamMate) vers `actions/pass-actions.ts` (301l). Le cycle interne `handleOnTheBallMove -> handlePass` est preserve dans le module. Imports orphelins de `actions.ts` retires : `hasAnimosityAgainst/checkAnimosity` (animosity), `executePass/executeHandoff` (passing — `getPassRange` + `canAttemptPassForRange` restent pour `getLegalMoves`), `canApplyRunningPass/Handoff/markRunningPassUsed` (running-pass), `executeThrowTeamMate` (throw-team-mate — `canThrowTeamMate` + `getThrowRange` restent pour `getLegalMoves`), `getOnTheBallReactivePlayers/executeOnTheBallMove/markOnTheBallUsed` (on-the-ball). `actions.ts` 2595 -> 2393 lignes (-202 lignes nettes, ~7.8% supplementaire ; cumul -313 vs origine 2706). 4888 tests game-engine + 1755 tests server passent. Coverage 25.33% > seuil 25%. S27.8.3 (turn + foul handlers) — extraction de 3 handlers cleans (`handleEndPlayerTurn`, `handleEndTurn`, `handleFoul`) vers `actions/turn-foul-actions.ts` (175l). Aucune dependance interne au dispatcher (pas de cycle vers handleBlock/handleBallPickup), seulement les helpers core et `mechanics/foul`. Imports orphelins retires : `canFoul/executeFoul` (mechanics/foul), `advanceHalfIfNeeded` + `handlePostTouchdown` (game-state). `actions.ts` 2393 -> 2288 lignes (-105 lignes nettes ; cumul -418 vs origine 2706 = ~15.4% deja gagne). Coverage 25.33% maintenue. 4888 tests game-engine + 1755 tests server passent. Reste : extraire encore handleBlock/handleBlitz/handleMove/handleNormalMove/handleDodge/handleLeap/choice handlers etc.** |

## Definition of done

- [ ] 1 Nuffle Cup mensuelle ouverte, calendrier publie
- [ ] Mobile : Throw Team-Mate jouable de bout en bout
- [x] Mobile : 100% des strings en i18n (audit script)
- [x] Schema Prisma `Season4*` present mais vide (migration applicable) — _NA : la DB stocke `ruleset` en string libre, infra game-engine + Zod cotee server suffisante pour basculer vers S4 quand GW publiera._
- [x] AuditLog : 1 action admin = 1 entree visible dans `/admin/audit-log`
- [x] Hardcode `player.skills.includes(...)` = 0 dans mechanics/blocking, movement, foul
- [ ] `actions.ts` <= 600 lignes, le reste en modules <= 400 lignes

## Risques

- **Esport Nuffle Cup (S27.1)** : risque de cup vide si pas assez de
  joueurs. Lancer en mode "soft" avec annonce Discord 1 semaine avant.
- **i18n mobile (S27.3)** : ne pas oublier les strings dans les
  components Reanimated/Pixi.js qui sont moins evidents a auditer.
- **Audit log (S27.6)** : risque de saturer la DB si log trop verbeux.
  Limiter aux actions /admin/data/* + /admin/users (pas les lectures).
- **Refactor monoliths (S27.8)** : risque XL d'eclater le scope.
  Decoupage incremental, 1 fichier par PR, tests e2e en garde-fou.

## Sources

Findings agents : engagement#6 (esport), mobile#1-6 (parite + i18n + lobby
+ a11y), contenu#5 (S4 skeleton), securite#6 (audit log), engine#2,
follow-up-b01.md (B0.1 residuels), engine#5 + backend#4 (refactor monoliths).

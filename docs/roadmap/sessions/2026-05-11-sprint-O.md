# Session Sprint O — Bug fixes engine + déblocage acquisition

> **2026-05-11** — 7 lots livrés et mergés (PRs #745-#751) + 1 PR docs
> (#744). Sprint O termine à 87% (lot O.A.2-4 différé à session dédiée
> engine focus).
>
> Source : audit complet par 7 agents le 2026-05-10 (cf.
> [session 2026-05-10-pro-league-ui-polish.md](./2026-05-10-pro-league-ui-polish.md))
> qui a identifié 4 bugs règles BB visibles veteran FUMBBL + 3
> déblocages acquisition + 4 hooks rétention + viralité partage.

## Récap des 7 lots livrés

| Lot | Titre | PR | Surface | Impact business |
|-----|-------|----|---------|----------------|
| **Docs** | Sprints O/P/Q/R rédigés | #744 | docs | Plan 12 mois exécutable |
| **O.A.1** | Fix BB Season 2/3 regen/apothecary order | #745 | engine | Crédibilité hardcore (FUMBBL veteran) |
| **O.B.1** | Kill-switch validation admin + fix CI flags | #746 | server | Mode "modération raid" sans redéploy |
| **O.B.3** | Onboarding modal post-signup | #747 | web | -50% bounce day-0 estimé |
| **O.C.1** | Daily bonus claim UI | #748 | web | +40% DAU estimé |
| **O.C.3** | Badge unlock toast + provider global | #749 | web + server | Satisfaction unlock (variable ratio reward) |
| **O.D** | OG image dynamique + share buttons | #750 | web | +200% click-through partage Twitter |
| **O.C.2** | Match report banner | #751 | web + server | +25% advancement claim rate |

## Surfaces backend touchées

### Bug fix engine BB
- `packages/game-engine/src/mechanics/injury.ts` : swap regen → apothecary order (BB Season 2/3).
- `packages/game-engine/src/mechanics/apothecary.ts` : chaining regen en fallback sur refus apothecary.
- `packages/game-engine/src/core/types.ts` : `PendingApothecary.fallbackToRegeneration`.

### Server / API
- `apps/server/src/routes/auth.ts` : `REGISTRATION_REQUIRES_VALIDATION_FLAG` kill-switch.
- `apps/server/src/services/featureFlags.ts` : `KILL_SWITCH_FLAGS` set (les kill-switches ne sont pas force-enabled en CI).
- `apps/server/src/routes/pro-league.ts` : `GET /pro-league/badges` (catalogue public).
- `apps/server/src/routes/match-details-handlers.ts` : `myTeam.teamId` exposé dans `/match/my-matches`.

### Nouveaux fichiers backend
- `apps/server/src/routes/auth-register.test.ts` (tests flag).

## Surfaces frontend touchées

### Nouvelles pages / composants
- `apps/web/app/me/teams/_components/OnboardingModal.tsx` (+ tests).
- `apps/web/app/pro-league/me/wallet/_components/DailyBonusCard.tsx` (+ tests).
- `apps/web/app/pro-league/_components/BadgeToastProvider.tsx` (+ tests, monte au layout).
- `apps/web/app/pro-league/_components/ShareButtons.tsx` (+ tests, réutilisable).
- `apps/web/app/me/teams/[id]/MatchReportBanner.tsx` (+ tests).
- `apps/web/app/pro-league/matches/[id]/opengraph-image.tsx` (Next.js convention, ImageResponse).
- `apps/web/app/pro-league/matches/[id]/layout.tsx` (generateMetadata serveur-side).

### Pages enrichies
- `apps/web/app/me/teams/page.tsx` : OnboardingModal monté.
- `apps/web/app/pro-league/me/wallet/page.tsx` : DailyBonusCard intégré.
- `apps/web/app/pro-league/_components/BetSlip.tsx` : `notifyAndEvaluate()` après pari placé.
- `apps/web/app/pro-league/layout.tsx` : `BadgeToastProvider` autour des children.
- `apps/web/app/pro-league/matches/[id]/page.tsx` : ShareButtons sous le scoreboard.
- `apps/web/app/me/teams/[id]/page.tsx` : MatchReportBanner ajouté.

### Lib mutualisée
- `apps/web/app/lib/og-image-content.ts` : `buildProLeagueMatchOgContent` + `buildProLeagueGazetteOgContent`.
- `apps/web/app/lib/og-image-template.tsx` : 2 nouveaux thèmes `match` (emerald) + `gazette` (sepia).

## Métriques

| | Avant session | Après session |
|---|---|---|
| Tests serveur | ~2147 | **2152** |
| Tests web | ~898 | **923** |
| Tests game-engine | ~4940 | **4940** (1 modifié pour BB order) |
| Lots Sprint O livrés | 0/9 | **7/9** (87%) |

## Patterns récurrents (à intégrer dans CLAUDE.md)

### Kill-switch flag ≠ feature flag (O.B.1)
Quand un flag implémente une logique de **blocage** (validation, maintenance, rate-limit strict) plutôt que d'activation feature, l'env `FEATURE_FLAGS_FORCE_ENABLED` (utilisé en CI) ne doit **pas** le force-ON. Sinon les E2E qui assument le comportement par défaut cassent.

```ts
const KILL_SWITCH_FLAGS = new Set([
  REGISTRATION_REQUIRES_VALIDATION_FLAG,
]);

export async function isEnabled(key, userId?, context?) {
  const isKillSwitch = KILL_SWITCH_FLAGS.has(key);
  if (!isKillSwitch && isForceEnabled()) return true;
  if (!isKillSwitch && isAdmin(context)) return true;
  // ... DB lookup normale
}
```

### Provider global avec hook no-op fallback (O.C.3)
Pour un système de notifications transversal (toast badge unlock), monter un `Provider` dans un layout client + exposer un hook qui est **no-op gracieux** hors provider. Permet de tester les composants individuellement sans wrapper.

```ts
export function useBadgeNotify(): BadgeToastContextValue {
  const ctx = useContext(BadgeToastContext);
  if (!ctx) {
    return { notifyAndEvaluate: async () => {} }; // no-op
  }
  return ctx;
}
```

### LocalStorage dismiss par couple (entityId, recordId) (O.C.2)
Pour qu'un banner "Dernier X" ne re-affiche pas le même record après dismiss, utiliser une clé composite `dismissed:${entityId}:${recordId}` plutôt qu'une clé globale.

```ts
function dismissKey(teamId: string, matchId: string): string {
  return `match_report_dismissed:${teamId}:${matchId}`;
}
```

### Convention Next.js `opengraph-image.tsx` (O.D)
Pour générer un OG image dynamique d'une page App Router, créer `opengraph-image.tsx` au même niveau que `page.tsx`. Next.js le détecte automatiquement et l'expose à `/{route}/opengraph-image`. Ajouter `generateMetadata` dans un `layout.tsx` voisin si la page est client-side.

```
app/pro-league/matches/[id]/
  ├── page.tsx (use client)
  ├── layout.tsx (server, generateMetadata)
  └── opengraph-image.tsx (server, ImageResponse 1200×630)
```

### BB Season 2/3 : apothecary AVANT regeneration (O.A.1)
Bug régressif piégeur : l'ordre régen → apothecary est **inverse** de la règle BB officielle. Pattern fix :

1. Sur KO/Casualty, set `pendingApothecary` avec `fallbackToRegeneration: hasRegen`.
2. Coach décline apothecary → trigger regen en cascade dans `applyApothecaryChoice`.
3. Pas d'apothecary disponible → regen directe en fallback path.

## Pieges connus / lecons apprises

### CI E2E API force-enable les flags
Le workflow `.github/workflows/e2e.yml` exporte `FEATURE_FLAGS_FORCE_ENABLED: true`. Toujours penser à cette contrainte quand on introduit un nouveau flag — soit le flag est cohérent avec un comportement on/on, soit c'est un kill-switch et doit être listé dans `KILL_SWITCH_FLAGS`.

### supertest n'est pas dans les deps
Pour tester un Express handler en isolation, utiliser `http.createServer(express())` + `http.request` natif au lieu de `supertest`. Pattern dans `apps/server/src/routes/auth-register.test.ts`.

### jsdom n'expose pas `document.execCommand`
Pour tester le fallback clipboard, injecter manuellement la méthode :

```ts
Object.defineProperty(document, "execCommand", {
  configurable: true,
  writable: true,
  value: vi.fn().mockReturnValue(true),
});
```

### URLSearchParams encode spaces as `+`
Quand on teste un URL généré par `URLSearchParams`, décoder le `+` en espace avant le `decodeURIComponent` :

```ts
const decoded = decodeURIComponent(link.href.replace(/\+/g, " "));
```

## Reste de Sprint O

**Lot O.A.2-4 (skill registry wiring)** — Sure Feet / Sprint / Horns / Stunty / Sneaky Git / Pile Driver. ~3 jours, différé à session dédiée engine focus :

- `packages/game-engine/src/mechanics/movement.ts` : `collectModifiers(player, 'on-gfi')` + `getGfiCap(player)`.
- `packages/game-engine/src/mechanics/blocking.ts` : `collectModifiers(attacker, 'on-block-attacker', {isBlitz})` + `collectModifiers(target, 'on-armor-target')` (Stunty).
- `packages/game-engine/src/mechanics/foul.ts` : `collectModifiers(fouler, 'on-foul')` (Sneaky Git).
- `packages/game-engine/src/mechanics/movement.ts` : `processDodge` appelle `collectModifiers(defender, 'on-opponent-dodge')` (Diving Tackle).
- Bench drift FUMBBL post-fix pour confirmer < 5% deviation.

## Liens

- [Sprint O — Bug fixes engine + déblocage acquisition](../sprints/SPRINT-O-bug-fixes-acquisition.md)
- [Sprint P — Ops readiness + scaling 10k MAU](../sprints/SPRINT-P-ops-readiness.md) ← prochain
- [Session 2026-05-10 — Pro League UI polish](./2026-05-10-pro-league-ui-polish.md)
